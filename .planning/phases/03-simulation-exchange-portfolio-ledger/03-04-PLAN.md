---
phase: 03-simulation-exchange-portfolio-ledger
plan: 04
type: execute
wave: 3
depends_on: ["03-01", "03-03"]
files_modified:
  - src/polymarket_quant/services/portfolio_ledger.py
  - tests/unit/test_portfolio_ledger.py
autonomous: true
requirements: [SIM-03]
user_setup: []
must_haves:
  truths:
    - Ledger facts are separate from valuation snapshots.
    - Cash, positions, fills, and fees are recorded as factual accounting entries.
    - Realized and unrealized PnL are separate.
    - Default mark policy is conservative and documented in code.
    - Rewards are tracked separately and excluded from core PnL.
  artifacts:
    - `portfolio_ledger.py` applies fills to cash and token positions.
    - `portfolio_ledger.py` computes conservative marks and PnL snapshots.
    - Unit tests cover buy/sell fills, fees, realized PnL, unrealized PnL, and reward exclusion.
  key_links:
    - Plan 03-05 wires this service into the paper exchange flow.
---

<objective>
Implement the factual portfolio ledger and conservative valuation layer for paper trading: cash, token positions, fills, fees, realized PnL, unrealized PnL, and reward-separated attribution.

Purpose: Strategy behavior needs an auditable account state that can be recomputed from facts and marked conservatively from current market data.
Output: Portfolio ledger service and unit tests.
</objective>

<execution_context>
@/Users/wuwei/Documents/polymarketQuantification/.codex/get-shit-done/workflows/execute-plan.md
@/Users/wuwei/Documents/polymarketQuantification/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-simulation-exchange-portfolio-ledger/03-CONTEXT.md
@.planning/phases/03-simulation-exchange-portfolio-ledger/03-RESEARCH.md
@.planning/phases/03-simulation-exchange-portfolio-ledger/03-PATTERNS.md
@.planning/phases/03-simulation-exchange-portfolio-ledger/03-01-SUMMARY.md
@.planning/phases/03-simulation-exchange-portfolio-ledger/03-03-SUMMARY.md
@.planning/phases/02-historical-real-time-data-platform/02-VERIFICATION.md
</context>

<threat_model>
## Threat Model

| ID | Threat | Severity | Mitigation Required In This Plan |
|----|--------|----------|-----------------------------------|
| T-03-11 | Fees are omitted from cash/PnL, inflating strategy performance | high | Fill application must create fee cash entries and tests assert cash includes fee |
| T-03-12 | Unrealized and realized PnL are merged, hiding open exposure | high | Valuation output must expose separate `realized_pnl` and `unrealized_pnl` fields |
| T-03-13 | Mark policy is optimistic, overstating paper account equity | high | Default mark policy must prefer conservative side of market and tests assert bid-based long marks |
| T-03-14 | Rewards are mixed into core PnL | medium | Reward estimate must be a separate field and not added to `core_pnl` |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Apply fills to factual cash and position ledger entries</name>
  <files>src/polymarket_quant/services/portfolio_ledger.py, tests/unit/test_portfolio_ledger.py</files>
  <read_first>src/polymarket_quant/domain/simulation.py, src/polymarket_quant/storage/simulation_store.py, .planning/phases/03-simulation-exchange-portfolio-ledger/03-RESEARCH.md</read_first>
  <action>Create `PolymarketFeePolicy` with constructor field `fee_rate: Decimal = Decimal("0")` and method `calculate_fee(price: Decimal, size: Decimal, liquidity_role: LiquidityRole) -> Decimal`. For `LiquidityRole.TAKER`, use `size * fee_rate * price * (Decimal("1") - price)` and quantize to `Decimal("0.00001")`; for `LiquidityRole.MAKER`, return `Decimal("0")`. Create `PortfolioLedgerService` with constructor `__init__(self, store: Any | None = None, starting_cash: Decimal = Decimal("0"), fee_policy: PolymarketFeePolicy | None = None)`. Implement `apply_fill(fill: SimulatedFill, order: SimulatedOrder) -> LedgerUpdateResult`. If `fill.fee` is zero, calculate fee with `fee_policy`; otherwise preserve explicit `fill.fee`. For buy fills, cash delta is `-(fill.price * fill.size) - fee` and token position delta is `+fill.size`. For sell fills, cash delta is `+(fill.price * fill.size) - fee` and token position delta is `-fill.size`. Return `LedgerUpdateResult` with `cash_entry: CashLedgerEntry`, `position_entry: PositionLedgerEntry`, and `fee_entry: CashLedgerEntry | None`. If a store is present, call `insert_cash_entry` and `insert_position_entry`. Use reason strings exactly `FILL_NOTIONAL` and `FEE`.</action>
  <verify>pytest tests/unit/test_portfolio_ledger.py -q</verify>
  <acceptance_criteria>
    - `src/polymarket_quant/services/portfolio_ledger.py` contains `class PortfolioLedgerService`
    - `src/polymarket_quant/services/portfolio_ledger.py` contains `class PolymarketFeePolicy`
    - `src/polymarket_quant/services/portfolio_ledger.py` contains `def calculate_fee`
    - `src/polymarket_quant/services/portfolio_ledger.py` contains `def apply_fill`
    - `src/polymarket_quant/services/portfolio_ledger.py` contains `FILL_NOTIONAL`
    - `src/polymarket_quant/services/portfolio_ledger.py` contains `FEE`
    - `tests/unit/test_portfolio_ledger.py` contains `test_buy_fill_debits_cash_and_adds_position`
    - `tests/unit/test_portfolio_ledger.py` contains `test_sell_fill_credits_cash_and_reduces_position`
    - `tests/unit/test_portfolio_ledger.py` contains `test_fee_is_recorded_as_separate_cash_entry`
    - `tests/unit/test_portfolio_ledger.py` contains `test_taker_fee_uses_polymarket_fee_formula`
  </acceptance_criteria>
  <done>Fills produce auditable cash and position ledger facts including fees.</done>
</task>

<task type="auto">
  <name>Task 2: Compute conservative marks and unrealized PnL</name>
  <files>src/polymarket_quant/services/portfolio_ledger.py, tests/unit/test_portfolio_ledger.py</files>
  <read_first>src/polymarket_quant/services/portfolio_ledger.py, src/polymarket_quant/domain/market_data.py, src/polymarket_quant/domain/simulation.py</read_first>
  <action>Create `ConservativeMarkPolicy` with method `mark_long_token(bbo: BestBidAsk, last_trade: LastTrade | None = None) -> tuple[Decimal, str]`. It must return `bbo.best_bid` with reason `BEST_BID` when present, else `last_trade.price` with reason `LAST_TRADE_FALLBACK` when present, else `Decimal("0")` with reason `ZERO_FALLBACK`. Implement `value_position(position: PositionState, bbo: BestBidAsk, last_trade: LastTrade | None = None) -> ValuationSnapshot` with `mark_price`, `mark_reason`, `unrealized_pnl`, `realized_pnl`, `core_pnl`, `reward_pnl=Decimal("0")`, and `total_pnl = core_pnl`. Do not add rewards to `core_pnl`.</action>
  <verify>pytest tests/unit/test_portfolio_ledger.py -q</verify>
  <acceptance_criteria>
    - `src/polymarket_quant/services/portfolio_ledger.py` contains `class ConservativeMarkPolicy`
    - `src/polymarket_quant/services/portfolio_ledger.py` contains `BEST_BID`
    - `src/polymarket_quant/services/portfolio_ledger.py` contains `LAST_TRADE_FALLBACK`
    - `src/polymarket_quant/services/portfolio_ledger.py` contains `ZERO_FALLBACK`
    - `src/polymarket_quant/services/portfolio_ledger.py` contains `unrealized_pnl`
    - `src/polymarket_quant/services/portfolio_ledger.py` contains `realized_pnl`
    - `tests/unit/test_portfolio_ledger.py` contains `test_conservative_mark_uses_best_bid_for_long_position`
    - `tests/unit/test_portfolio_ledger.py` contains `test_rewards_are_not_included_in_core_pnl`
  </acceptance_criteria>
  <done>Valuation snapshots mark positions conservatively and keep rewards separate from core PnL.</done>
</task>

<task type="auto">
  <name>Task 3: Track realized PnL separately from unrealized PnL</name>
  <files>src/polymarket_quant/services/portfolio_ledger.py, tests/unit/test_portfolio_ledger.py</files>
  <read_first>src/polymarket_quant/services/portfolio_ledger.py, src/polymarket_quant/domain/simulation.py, .planning/phases/03-simulation-exchange-portfolio-ledger/03-CONTEXT.md</read_first>
  <action>Add position cost basis support to `PositionState` handling in `PortfolioLedgerService`. Implement `apply_position_change(position: PositionState, fill: SimulatedFill, order: SimulatedOrder) -> PositionState` so buys increase quantity and weighted average cost, sells reduce quantity and add realized PnL equal to `(fill.price - average_cost) * fill.size - fill.fee` for long token inventory. Preserve `unrealized_pnl` computation in `value_position`; do not move unrealized values into realized PnL.</action>
  <verify>pytest tests/unit/test_portfolio_ledger.py -q</verify>
  <acceptance_criteria>
    - `src/polymarket_quant/services/portfolio_ledger.py` contains `def apply_position_change`
    - `src/polymarket_quant/services/portfolio_ledger.py` contains `average_cost`
    - `tests/unit/test_portfolio_ledger.py` contains `test_sell_fill_realizes_pnl_against_average_cost`
    - `tests/unit/test_portfolio_ledger.py` contains `test_unrealized_pnl_remains_separate_from_realized_pnl`
  </acceptance_criteria>
  <done>Ledger can distinguish closed-trade realized PnL from open-position mark-to-market PnL.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/unit/test_portfolio_ledger.py -q` passes
- [ ] `pytest -q` passes
- [ ] `rg "realized_pnl|unrealized_pnl|ConservativeMarkPolicy|BEST_BID" src/polymarket_quant/services/portfolio_ledger.py` finds required accounting behavior
- [ ] `rg "test_rewards_are_not_included_in_core_pnl|test_sell_fill_realizes_pnl_against_average_cost" tests/unit/test_portfolio_ledger.py` finds required tests
</verification>

<success_criteria>
- Fills produce cash and token position facts
- Fees are recorded as separate cash entries
- Conservative mark uses bid-first long-token valuation
- Realized and unrealized PnL remain separate
- Rewards are not included in core PnL
</success_criteria>

<output>
After completion, create `.planning/phases/03-simulation-exchange-portfolio-ledger/03-04-SUMMARY.md`
</output>
