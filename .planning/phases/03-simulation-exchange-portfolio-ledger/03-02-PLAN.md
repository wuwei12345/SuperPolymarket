---
phase: 03-simulation-exchange-portfolio-ledger
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/polymarket_quant/services/order_lifecycle.py
  - src/polymarket_quant/services/order_risk.py
  - tests/unit/test_order_risk.py
autonomous: true
requirements: [SIM-01, RISK-01]
user_setup: []
must_haves:
  truths:
    - Pre-trade risk checks run before simulated order acceptance.
    - Balance, size, tick, single-order, single-market, and single-token position checks are hard rejects in P0.
    - Portfolio exposure, event exposure, drawdown, and liquidity anomaly checks are warnings in P0.
    - Every risk result is a structured `RiskDecision`.
    - Order lifecycle prevents invalid transitions.
  artifacts:
    - `order_lifecycle.py` contains a deterministic state transition service.
    - `order_risk.py` contains validation and risk decision logic.
    - `test_order_risk.py` covers hard rejects, warnings, and transition failures.
  key_links:
    - Plan 03-05 uses these services before `SimulationStore.upsert_order`.
---

<objective>
Implement P0 order lifecycle and risk validation for paper orders: convert `OrderIntent` into accepted/rejected simulated orders only after structured risk checks, and enforce legal state transitions for cancel/replace/fill lifecycle events.

Purpose: The simulator must reject invalid orders before simulated acceptance and record why orders were allowed, warned, or rejected.
Output: Order lifecycle service, risk engine, and unit tests.
</objective>

<execution_context>
@/Users/wuwei/Documents/polymarketQuantification/.codex/get-shit-done/workflows/execute-plan.md
@/Users/wuwei/Documents/polymarketQuantification/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-simulation-exchange-portfolio-ledger/03-CONTEXT.md
@.planning/phases/03-simulation-exchange-portfolio-ledger/03-RESEARCH.md
@.planning/phases/03-simulation-exchange-portfolio-ledger/03-PATTERNS.md
@.planning/phases/03-simulation-exchange-portfolio-ledger/03-01-SUMMARY.md
</context>

<threat_model>
## Threat Model

| ID | Threat | Severity | Mitigation Required In This Plan |
|----|--------|----------|-----------------------------------|
| T-03-05 | Invalid tick size, minimum size, or balance is accepted and later creates impossible fills | high | `apply_risk_checks` must hard reject tick, size, balance, and position violations with machine-readable reasons |
| T-03-06 | Warning-class aggregate risk blocks P0 research unexpectedly | medium | Portfolio/event/drawdown/liquidity checks must produce `WARN`, not `REJECT`, in P0 |
| T-03-07 | Invalid lifecycle transitions corrupt order state | high | State transition helper must reject illegal transitions and tests cover examples |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Implement deterministic order lifecycle transitions</name>
  <files>src/polymarket_quant/services/order_lifecycle.py, tests/unit/test_order_risk.py</files>
  <read_first>src/polymarket_quant/domain/simulation.py, .planning/phases/03-simulation-exchange-portfolio-ledger/03-CONTEXT.md</read_first>
  <action>Create `OrderLifecycleService` with methods `create_order(intent: OrderIntent, now: datetime | None = None) -> SimulatedOrder`, `transition(order: SimulatedOrder, new_status: OrderStatus, reason: str, now: datetime | None = None) -> tuple[SimulatedOrder, OrderStateTransition]`, `request_cancel(order, now=None)`, `cancel(order, now=None)`, `request_replace(order, now=None)`, `reject(order, reason, now=None)`, `mark_open(order, now=None)`, `mark_partially_filled(order, remaining_size: Decimal, now=None)`, and `mark_filled(order, now=None)`. Encode legal transitions so `PENDING -> ACCEPTED -> OPEN -> PARTIALLY_FILLED -> FILLED`, `OPEN -> CANCEL_REQUESTED -> CANCELED`, and `OPEN -> REPLACE_REQUESTED -> REPLACED` are allowed. Reject `FILLED -> CANCELED`, `REJECTED -> OPEN`, and `CANCELED -> FILLED` by raising `ValueError` with message containing `invalid order transition`.</action>
  <verify>pytest tests/unit/test_order_risk.py -q</verify>
  <acceptance_criteria>
    - `src/polymarket_quant/services/order_lifecycle.py` contains `class OrderLifecycleService`
    - `src/polymarket_quant/services/order_lifecycle.py` contains `invalid order transition`
    - `src/polymarket_quant/services/order_lifecycle.py` contains `def request_cancel`
    - `src/polymarket_quant/services/order_lifecycle.py` contains `def request_replace`
    - `tests/unit/test_order_risk.py` contains `test_lifecycle_rejects_filled_to_canceled_transition`
  </acceptance_criteria>
  <done>Order lifecycle has explicit legal transitions and rejects invalid state changes.</done>
</task>

<task type="auto">
  <name>Task 2: Implement hard pre-trade risk checks</name>
  <files>src/polymarket_quant/services/order_risk.py, tests/unit/test_order_risk.py</files>
  <read_first>src/polymarket_quant/domain/simulation.py, src/polymarket_quant/domain/market_data.py, .planning/phases/03-simulation-exchange-portfolio-ledger/03-RESEARCH.md</read_first>
  <action>Create `RiskLimits` dataclass or Pydantic model with fields `cash_available: Decimal`, `token_position: Decimal = Decimal("0")`, `max_single_order_notional: Decimal`, `max_market_position: Decimal`, `max_token_position: Decimal`, `portfolio_exposure_warning: Decimal | None = None`, `event_exposure_warning: Decimal | None = None`, `drawdown_warning: Decimal | None = None`, and `liquidity_warning_spread: Decimal | None = None`. Create `MarketConstraints` with `token_id`, `condition_id`, `tick_size`, `min_order_size`, `best_bid`, `best_ask`, `spread`, `active: bool = True`, and `accepting_orders: bool = True`. Implement `validate_order_intent(intent, constraints) -> list[RiskCheckResult]` and `apply_risk_checks(intent: OrderIntent, constraints: MarketConstraints, limits: RiskLimits) -> RiskDecision`. Hard reject with reason codes exactly `INVALID_TICK_SIZE`, `BELOW_MIN_ORDER_SIZE`, `INSUFFICIENT_CASH`, `INSUFFICIENT_POSITION`, `MAX_SINGLE_ORDER_EXCEEDED`, `MAX_MARKET_POSITION_EXCEEDED`, `MAX_TOKEN_POSITION_EXCEEDED`, and `MARKET_NOT_ACCEPTING_ORDERS` where applicable.</action>
  <verify>pytest tests/unit/test_order_risk.py -q</verify>
  <acceptance_criteria>
    - `src/polymarket_quant/services/order_risk.py` contains `class RiskLimits`
    - `src/polymarket_quant/services/order_risk.py` contains `class MarketConstraints`
    - `src/polymarket_quant/services/order_risk.py` contains `def validate_order_intent`
    - `src/polymarket_quant/services/order_risk.py` contains `def apply_risk_checks`
    - `src/polymarket_quant/services/order_risk.py` contains `INVALID_TICK_SIZE`
    - `src/polymarket_quant/services/order_risk.py` contains `INSUFFICIENT_CASH`
    - `tests/unit/test_order_risk.py` contains `test_risk_rejects_invalid_tick_size`
    - `tests/unit/test_order_risk.py` contains `test_risk_rejects_insufficient_cash_for_buy`
  </acceptance_criteria>
  <done>P0 hard risk checks return structured rejection decisions before order acceptance.</done>
</task>

<task type="auto">
  <name>Task 3: Implement warning-class aggregate risk checks</name>
  <files>src/polymarket_quant/services/order_risk.py, tests/unit/test_order_risk.py</files>
  <read_first>src/polymarket_quant/services/order_risk.py, src/polymarket_quant/domain/simulation.py, .planning/phases/03-simulation-exchange-portfolio-ledger/03-CONTEXT.md</read_first>
  <action>Extend `apply_risk_checks` so portfolio exposure, event exposure, drawdown, and liquidity anomaly thresholds produce `RiskDecisionType.WARN` when no hard reject exists. Use warning reason codes exactly `PORTFOLIO_EXPOSURE_WARNING`, `EVENT_EXPOSURE_WARNING`, `DRAWDOWN_WARNING`, and `LIQUIDITY_ANOMALY_WARNING`. Keep the decision as `REJECT` if any hard reject reason exists. Include both `reasons` and `warnings` lists in the `RiskDecision`.</action>
  <verify>pytest tests/unit/test_order_risk.py -q</verify>
  <acceptance_criteria>
    - `src/polymarket_quant/services/order_risk.py` contains `PORTFOLIO_EXPOSURE_WARNING`
    - `src/polymarket_quant/services/order_risk.py` contains `LIQUIDITY_ANOMALY_WARNING`
    - `tests/unit/test_order_risk.py` contains `test_warning_checks_do_not_hard_reject_order`
    - `tests/unit/test_order_risk.py` contains `test_hard_reject_takes_priority_over_warnings`
  </acceptance_criteria>
  <done>Aggregate risk emits warnings without blocking P0 orders unless a hard reject is present.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/unit/test_order_risk.py -q` passes
- [ ] `pytest -q` passes
- [ ] `rg "def validate_order_intent|def apply_risk_checks|RiskLimits|MarketConstraints" src/polymarket_quant/services/order_risk.py` finds required risk APIs
- [ ] `rg "invalid order transition|def request_cancel|def request_replace" src/polymarket_quant/services/order_lifecycle.py` finds lifecycle APIs
</verification>

<success_criteria>
- Limit order intents are validated before acceptance
- Hard risk violations reject orders with structured reason codes
- Aggregate risk emits warning decisions in P0
- Invalid order state transitions raise deterministic errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-simulation-exchange-portfolio-ledger/03-02-SUMMARY.md`
</output>
