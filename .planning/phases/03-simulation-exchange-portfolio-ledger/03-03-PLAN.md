---
phase: 03-simulation-exchange-portfolio-ledger
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/polymarket_quant/services/fill_engine.py
  - tests/unit/test_fill_engine.py
autonomous: true
requirements: [SIM-02]
user_setup: []
must_haves:
  truths:
    - P0 paper fills are driven by orderbook depth, not last trade alone.
    - Buy orders consume asks; sell orders consume bids.
    - Limit price gates every fill level.
    - Partial fills leave residual size open.
    - Submit/cancel latency is configurable and affects eligibility.
    - Conservative queue assumptions prevent optimistic resting-order fills.
  artifacts:
    - `fill_engine.py` implements deterministic fill simulation.
    - Tests cover marketable buys, marketable sells, partial fills, no-cross residuals, latency, and conservative resting behavior.
  key_links:
    - Plan 03-05 calls this engine after risk acceptance.
---

<objective>
Implement the P0 paper fill engine that converts accepted limit or marketable-limit orders into deterministic simulated fills using Phase 2 book depth and conservative queue rules.

Purpose: The simulator needs realistic enough fill behavior to avoid top-of-book-only or last-trade-only optimism while staying within research-grade replay limits.
Output: Fill engine service and unit tests.
</objective>

<execution_context>
@/Users/wuwei/Documents/polymarketQuantification/.codex/get-shit-done/workflows/execute-plan.md
@/Users/wuwei/Documents/polymarketQuantification/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-simulation-exchange-portfolio-ledger/03-CONTEXT.md
@.planning/phases/03-simulation-exchange-portfolio-ledger/03-RESEARCH.md
@.planning/phases/03-simulation-exchange-portfolio-ledger/03-PATTERNS.md
@.planning/phases/03-simulation-exchange-portfolio-ledger/03-01-SUMMARY.md
@.planning/phases/02-historical-real-time-data-platform/02-VERIFICATION.md
</context>

<threat_model>
## Threat Model

| ID | Threat | Severity | Mitigation Required In This Plan |
|----|--------|----------|-----------------------------------|
| T-03-08 | Fill engine overfills available depth or ignores residual quantity | high | Tests must assert partial fills and remaining size calculations |
| T-03-09 | Non-marketable resting orders fill immediately without queue proof | high | Conservative queue policy must prevent same-snapshot resting fills |
| T-03-10 | Cancel latency is ignored, producing impossible post-cancel fills or no fills | medium | Engine must expose cancel latency eligibility helpers and tests |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Implement depth-driven marketable-limit matching</name>
  <files>src/polymarket_quant/services/fill_engine.py, tests/unit/test_fill_engine.py</files>
  <read_first>src/polymarket_quant/domain/simulation.py, src/polymarket_quant/domain/market_data.py, .planning/phases/03-simulation-exchange-portfolio-ledger/03-RESEARCH.md</read_first>
  <action>Create `FillEngineConfig` with `submit_latency_ms: int = 250`, `cancel_latency_ms: int = 250`, and `conservative_queue: bool = True`. Create `FillSimulationResult` with `fills: list[SimulatedFill]`, `remaining_size: Decimal`, `filled_size: Decimal`, `status: OrderStatus`, and `reason: str`. Implement `simulate_fills(order: SimulatedOrder, snapshot: BookSnapshot, config: FillEngineConfig, now: datetime | None = None) -> FillSimulationResult`. For `OrderSide.BUY`, consume `snapshot.asks` in ascending price order where `level.price <= order.price`. For `OrderSide.SELL`, consume `snapshot.bids` in descending price order where `level.price >= order.price`. For each fill, create `SimulatedFill` with `liquidity_role=LiquidityRole.TAKER`, `fee=Decimal("0")` as placeholder for Plan 04 fee calculation, `source_snapshot_id=None`, and `source_ts=snapshot.source_ts`. Stop when order size is filled or no eligible depth remains.</action>
  <verify>pytest tests/unit/test_fill_engine.py -q</verify>
  <acceptance_criteria>
    - `src/polymarket_quant/services/fill_engine.py` contains `class FillEngineConfig`
    - `src/polymarket_quant/services/fill_engine.py` contains `class FillSimulationResult`
    - `src/polymarket_quant/services/fill_engine.py` contains `def simulate_fills`
    - `src/polymarket_quant/services/fill_engine.py` contains `snapshot.asks`
    - `src/polymarket_quant/services/fill_engine.py` contains `snapshot.bids`
    - `tests/unit/test_fill_engine.py` contains `test_marketable_buy_consumes_asks_by_price`
    - `tests/unit/test_fill_engine.py` contains `test_marketable_sell_consumes_bids_by_price`
  </acceptance_criteria>
  <done>Marketable-limit orders fill against eligible book depth in deterministic price order.</done>
</task>

<task type="auto">
  <name>Task 2: Support partial fills and residual open quantity</name>
  <files>src/polymarket_quant/services/fill_engine.py, tests/unit/test_fill_engine.py</files>
  <read_first>src/polymarket_quant/services/fill_engine.py, src/polymarket_quant/domain/simulation.py, src/polymarket_quant/domain/market_data.py</read_first>
  <action>Extend `simulate_fills` so when available eligible depth is smaller than `order.remaining_size`, it returns `status=OrderStatus.PARTIALLY_FILLED`, `filled_size` equal to consumed depth, and `remaining_size` equal to original remaining minus consumed depth. When no eligible depth exists, return no fills, original remaining size, `status=OrderStatus.OPEN`, and `reason="NO_CROSS"`. When fully filled, return `status=OrderStatus.FILLED` and `remaining_size=Decimal("0")`.</action>
  <verify>pytest tests/unit/test_fill_engine.py -q</verify>
  <acceptance_criteria>
    - `src/polymarket_quant/services/fill_engine.py` contains `PARTIALLY_FILLED`
    - `src/polymarket_quant/services/fill_engine.py` contains `NO_CROSS`
    - `tests/unit/test_fill_engine.py` contains `test_partial_fill_leaves_residual_open`
    - `tests/unit/test_fill_engine.py` contains `test_non_crossing_limit_order_remains_open`
    - `tests/unit/test_fill_engine.py` contains `test_full_fill_sets_remaining_size_to_zero`
  </acceptance_criteria>
  <done>Fill engine handles full, partial, and no-cross outcomes without overfilling.</done>
</task>

<task type="auto">
  <name>Task 3: Add latency and conservative queue rules</name>
  <files>src/polymarket_quant/services/fill_engine.py, tests/unit/test_fill_engine.py</files>
  <read_first>src/polymarket_quant/services/fill_engine.py, .planning/phases/03-simulation-exchange-portfolio-ledger/03-CONTEXT.md, .planning/phases/03-simulation-exchange-portfolio-ledger/03-RESEARCH.md</read_first>
  <action>Add helper `is_submission_effective(order: SimulatedOrder, now: datetime, config: FillEngineConfig) -> bool` that returns true only when `now >= order.created_at + submit_latency_ms`. Add helper `is_cancel_effective(cancel_requested_at: datetime, now: datetime, config: FillEngineConfig) -> bool` that returns true only when `now >= cancel_requested_at + cancel_latency_ms`. Update `simulate_fills` to return `reason="SUBMIT_LATENCY_PENDING"` and no fills if submission is not effective. Add conservative queue behavior: when `config.conservative_queue is True` and an order was not marketable against the current snapshot, do not infer maker fills from the same snapshot; leave it `OPEN` with `reason="CONSERVATIVE_QUEUE_WAIT"`.</action>
  <verify>pytest tests/unit/test_fill_engine.py -q</verify>
  <acceptance_criteria>
    - `src/polymarket_quant/services/fill_engine.py` contains `def is_submission_effective`
    - `src/polymarket_quant/services/fill_engine.py` contains `def is_cancel_effective`
    - `src/polymarket_quant/services/fill_engine.py` contains `SUBMIT_LATENCY_PENDING`
    - `src/polymarket_quant/services/fill_engine.py` contains `CONSERVATIVE_QUEUE_WAIT`
    - `tests/unit/test_fill_engine.py` contains `test_submit_latency_blocks_immediate_fill`
    - `tests/unit/test_fill_engine.py` contains `test_cancel_latency_helper_requires_delay`
    - `tests/unit/test_fill_engine.py` contains `test_conservative_queue_does_not_fill_resting_order_from_same_snapshot`
  </acceptance_criteria>
  <done>Latency and conservative queue assumptions are explicit and test-covered.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/unit/test_fill_engine.py -q` passes
- [ ] `pytest -q` passes
- [ ] `rg "def simulate_fills|PARTIALLY_FILLED|SUBMIT_LATENCY_PENDING|CONSERVATIVE_QUEUE_WAIT" src/polymarket_quant/services/fill_engine.py` finds required fill behavior
</verification>

<success_criteria>
- Fill engine consumes eligible book depth by side and price
- Partial fills leave accurate residual quantity
- Submit/cancel latency helpers are configurable and tested
- Conservative queue behavior prevents optimistic same-snapshot maker fills
</success_criteria>

<output>
After completion, create `.planning/phases/03-simulation-exchange-portfolio-ledger/03-03-SUMMARY.md`
</output>
