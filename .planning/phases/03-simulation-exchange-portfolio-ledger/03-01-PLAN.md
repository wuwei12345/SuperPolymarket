---
phase: 03-simulation-exchange-portfolio-ledger
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/polymarket_quant/domain/__init__.py
  - src/polymarket_quant/domain/simulation.py
  - src/polymarket_quant/storage/__init__.py
  - src/polymarket_quant/storage/postgres_schema.sql
  - src/polymarket_quant/storage/simulation_store.py
  - tests/unit/test_simulation_models.py
  - tests/unit/test_simulation_store.py
autonomous: true
requirements: [SIM-01, SIM-03, RISK-01]
user_setup:
  - "For live PostgreSQL smoke only: set DATABASE_URL=postgresql://localhost/polymarket_quant or another disposable PostgreSQL DSN."
must_haves:
  truths:
    - Phase 3 adds simulation-specific domain models without mutating Phase 1 market models or Phase 2 market-data models.
    - Limit order is the only P0 order type; marketable limit remains represented as a limit order.
    - `client_order_id` is a first-class identifier for traceability and idempotency.
    - Simulation facts and valuation snapshots are stored separately.
    - Risk decisions are structured records, not ad hoc strings.
  artifacts:
    - `src/polymarket_quant/domain/simulation.py` defines order, risk, fill, ledger, and valuation DTOs.
    - `src/polymarket_quant/storage/postgres_schema.sql` defines `simulation` tables and query views.
    - `src/polymarket_quant/storage/simulation_store.py` exposes insert/fetch methods for simulation records.
    - Unit tests verify model validation and repository SQL contracts without live PostgreSQL.
  key_links:
    - Later plans consume `OrderIntent`, `RiskDecision`, `SimulatedFill`, ledger DTOs, and `SimulationStore`.
---

<objective>
Create the Phase 3 simulation foundation: typed order/risk/fill/ledger models, PostgreSQL simulation schema, and a testable repository for paper exchange facts and valuation snapshots.

Purpose: Order validation, fill simulation, risk checks, and portfolio accounting all need stable shared contracts before execution services are implemented.
Output: Domain models, `simulation` schema SQL, `SimulationStore`, exports, and unit tests.
</objective>

<execution_context>
@/Users/wuwei/Documents/polymarketQuantification/.codex/get-shit-done/workflows/execute-plan.md
@/Users/wuwei/Documents/polymarketQuantification/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/03-simulation-exchange-portfolio-ledger/03-CONTEXT.md
@.planning/phases/03-simulation-exchange-portfolio-ledger/03-RESEARCH.md
@.planning/phases/03-simulation-exchange-portfolio-ledger/03-VALIDATION.md
@.planning/phases/03-simulation-exchange-portfolio-ledger/03-PATTERNS.md
@.planning/phases/02-historical-real-time-data-platform/02-VERIFICATION.md
</context>

<threat_model>
## Threat Model

| ID | Threat | Severity | Mitigation Required In This Plan |
|----|--------|----------|-----------------------------------|
| T-03-01 | Strategy orders cannot be traced or deduplicated because `client_order_id` is optional or missing | high | `OrderIntent` and `SimulatedOrder` must require non-blank `client_order_id`; tests reject blanks |
| T-03-02 | Ledger facts and valuation snapshots are mixed, preventing recalculation of marks/PnL | high | Schema and DTOs must separate order/fill/cash/position facts from valuation snapshots |
| T-03-03 | Risk decisions become unstructured errors that later services cannot audit | high | Define `RiskDecision` and persist `simulation.risk_decisions` JSON check details |
| T-03-04 | Phase 3 schema changes break Phase 2 market-data storage | medium | Append `simulation` schema/tables; do not remove or rename existing `reference/raw/normalized/views` tables |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add simulation domain models</name>
  <files>src/polymarket_quant/domain/__init__.py, src/polymarket_quant/domain/simulation.py, tests/unit/test_simulation_models.py</files>
  <read_first>src/polymarket_quant/domain/market.py, src/polymarket_quant/domain/market_data.py, .planning/phases/03-simulation-exchange-portfolio-ledger/03-CONTEXT.md, .planning/phases/03-simulation-exchange-portfolio-ledger/03-RESEARCH.md</read_first>
  <action>Create `src/polymarket_quant/domain/simulation.py` using Pydantic v2 with `ConfigDict(extra="forbid")`. Define enums `OrderSide` with `BUY` and `SELL`, `OrderType` with `LIMIT`, `TimeInForce` with `GTC` and `GTD`, `OrderStatus` with `PENDING`, `ACCEPTED`, `OPEN`, `PARTIALLY_FILLED`, `FILLED`, `CANCEL_REQUESTED`, `CANCELED`, `REPLACE_REQUESTED`, `REPLACED`, `REJECTED`, and `EXPIRED`, `LiquidityRole` with `MAKER` and `TAKER`, and `RiskDecisionType` with `ALLOW`, `WARN`, `REJECT`. Define models `OrderIntent`, `SimulatedOrder`, `OrderStateTransition`, `SimulatedFill`, `CashLedgerEntry`, `PositionLedgerEntry`, `PositionState`, `ValuationSnapshot`, `RiskCheckResult`, `RiskDecision`, and `SimulationConfig`. Required `OrderIntent` fields: `client_order_id: str`, `strategy_id: str`, `token_id: str`, `condition_id: str | None`, `side: OrderSide`, `order_type: OrderType = OrderType.LIMIT`, `price: Decimal`, `size: Decimal`, `time_in_force: TimeInForce = TimeInForce.GTC`, `post_only: bool = False`, `expires_at: datetime | None = None`, `created_at: datetime`. Required `SimulatedOrder` fields: all `OrderIntent` identity/order fields plus `remaining_size: Decimal`, `status: OrderStatus`, `accepted_at: datetime | None`, `updated_at: datetime`, and `reject_reason: str | None`. Required `SimulatedFill` fields: `fill_id: str`, `client_order_id: str`, `strategy_id: str`, `token_id: str`, `condition_id: str | None`, `side: OrderSide`, `price: Decimal`, `size: Decimal`, `fee: Decimal = Decimal("0")`, `liquidity_role: LiquidityRole`, `source_snapshot_id: int | None = None`, `source_ts: datetime | None = None`, and `created_at: datetime`. Required `PositionState` fields: `strategy_id`, `token_id`, `condition_id`, `quantity: Decimal`, `average_cost: Decimal`, `realized_pnl: Decimal`, and `fees_paid: Decimal`. Required `ValuationSnapshot` fields: `strategy_id`, `token_id`, `condition_id`, `quantity`, `average_cost`, `mark_price`, `mark_reason`, `realized_pnl`, `unrealized_pnl`, `core_pnl`, `reward_pnl`, `total_pnl`, and `created_at`. Required `RiskCheckResult` fields: `code`, `passed`, `severity`, `message`, and `details: dict[str, object] = Field(default_factory=dict)`. Required `RiskDecision` fields: `decision`, `client_order_id`, `token_id`, `condition_id`, `checks`, `reasons`, `warnings`, and `created_at`. Add validators rejecting blank `client_order_id`, `strategy_id`, and `token_id`; reject `price < 0`, `price > 1`, and `size <= 0`. Export new symbols from `domain/__init__.py`.</action>
  <verify>pytest tests/unit/test_simulation_models.py -q</verify>
  <acceptance_criteria>
    - `src/polymarket_quant/domain/simulation.py` contains `class OrderIntent`
    - `src/polymarket_quant/domain/simulation.py` contains `class RiskDecision`
    - `src/polymarket_quant/domain/simulation.py` contains `class SimulatedFill`
    - `src/polymarket_quant/domain/simulation.py` contains `class ValuationSnapshot`
    - `src/polymarket_quant/domain/simulation.py` contains `LIMIT`
    - `tests/unit/test_simulation_models.py` contains `test_order_intent_rejects_blank_client_order_id`
    - `tests/unit/test_simulation_models.py` contains `test_order_intent_rejects_price_above_one`
  </acceptance_criteria>
  <done>Simulation contracts exist and reject invalid identifiers/prices/sizes.</done>
</task>

<task type="auto">
  <name>Task 2: Extend PostgreSQL schema with simulation tables</name>
  <files>src/polymarket_quant/storage/postgres_schema.sql, tests/unit/test_simulation_store.py</files>
  <read_first>src/polymarket_quant/storage/postgres_schema.sql, .planning/phases/03-simulation-exchange-portfolio-ledger/03-CONTEXT.md, .planning/phases/03-simulation-exchange-portfolio-ledger/03-PATTERNS.md</read_first>
  <action>Append `CREATE SCHEMA IF NOT EXISTS simulation;` to `postgres_schema.sql`. Add tables `simulation.orders`, `simulation.order_transitions`, `simulation.fills`, `simulation.cash_ledger`, `simulation.position_ledger`, `simulation.risk_decisions`, and `simulation.valuation_snapshots`. `simulation.orders` must include `client_order_id TEXT UNIQUE NOT NULL`, `strategy_id TEXT NOT NULL`, `token_id TEXT NOT NULL`, `condition_id TEXT`, `side TEXT NOT NULL`, `order_type TEXT NOT NULL`, `price NUMERIC NOT NULL`, `size NUMERIC NOT NULL`, `remaining_size NUMERIC NOT NULL`, `status TEXT NOT NULL`, `time_in_force TEXT NOT NULL`, `post_only BOOLEAN NOT NULL DEFAULT false`, `created_at TIMESTAMPTZ NOT NULL`, `updated_at TIMESTAMPTZ NOT NULL`. `simulation.fills` must include `fill_id TEXT PRIMARY KEY`, `client_order_id TEXT NOT NULL`, `price NUMERIC NOT NULL`, `size NUMERIC NOT NULL`, `fee NUMERIC NOT NULL DEFAULT 0`, and `liquidity_role TEXT NOT NULL`. `simulation.risk_decisions` must include `decision TEXT NOT NULL`, `reasons TEXT[] NOT NULL`, `warnings TEXT[] NOT NULL`, and `checks JSONB NOT NULL`. Add views `views.simulation_positions` and `views.simulation_pnl`. Preserve all existing Phase 2 SQL objects.</action>
  <verify>pytest tests/unit/test_simulation_store.py -q</verify>
  <acceptance_criteria>
    - `src/polymarket_quant/storage/postgres_schema.sql` contains `CREATE SCHEMA IF NOT EXISTS simulation`
    - `src/polymarket_quant/storage/postgres_schema.sql` contains `simulation.orders`
    - `src/polymarket_quant/storage/postgres_schema.sql` contains `simulation.fills`
    - `src/polymarket_quant/storage/postgres_schema.sql` contains `simulation.risk_decisions`
    - `src/polymarket_quant/storage/postgres_schema.sql` contains `simulation.valuation_snapshots`
    - `src/polymarket_quant/storage/postgres_schema.sql` contains `views.simulation_pnl`
    - `src/polymarket_quant/storage/postgres_schema.sql` still contains `raw.websocket_events`
    - `tests/unit/test_simulation_store.py` contains `test_postgres_schema_defines_simulation_layer`
  </acceptance_criteria>
  <done>Database schema has isolated simulation tables and views while preserving market-data tables.</done>
</task>

<task type="auto">
  <name>Task 3: Implement SimulationStore repository</name>
  <files>src/polymarket_quant/storage/__init__.py, src/polymarket_quant/storage/simulation_store.py, tests/unit/test_simulation_store.py</files>
  <read_first>src/polymarket_quant/storage/market_data_store.py, src/polymarket_quant/storage/postgres_schema.sql, src/polymarket_quant/domain/simulation.py</read_first>
  <action>Create `SimulationStore` with constructor `__init__(self, dsn: str | None = None, connection: Any | None = None)` using `DATABASE_URL` fallback. Mirror the helper style from `MarketDataStore`: `_connect`, `_execute`, `_executemany`, `_fetch_all`, `_commit`, `_adapt_params`, and `_scalar` where useful. Implement `init_schema()`, `upsert_order(order: SimulatedOrder)`, `insert_order_transition(transition: OrderStateTransition)`, `insert_fill(fill: SimulatedFill)`, `insert_cash_entry(entry: CashLedgerEntry)`, `insert_position_entry(entry: PositionLedgerEntry)`, `insert_risk_decision(decision: RiskDecision)`, `insert_valuation_snapshot(snapshot: ValuationSnapshot)`, `fetch_order(client_order_id: str)`, `fetch_positions(strategy_id: str | None = None)`, and `fetch_pnl(strategy_id: str | None = None)`. Use JSONB adaptation for `RiskDecision.checks`. Export `SimulationStore` from `storage/__init__.py`.</action>
  <verify>pytest tests/unit/test_simulation_store.py -q</verify>
  <acceptance_criteria>
    - `src/polymarket_quant/storage/simulation_store.py` contains `class SimulationStore`
    - `src/polymarket_quant/storage/simulation_store.py` contains `def upsert_order`
    - `src/polymarket_quant/storage/simulation_store.py` contains `def insert_risk_decision`
    - `src/polymarket_quant/storage/simulation_store.py` contains `def insert_valuation_snapshot`
    - `src/polymarket_quant/storage/simulation_store.py` contains `Jsonb`
    - `tests/unit/test_simulation_store.py` contains `test_store_inserts_risk_decision_as_jsonb_checks`
    - `pytest tests/unit/test_simulation_store.py -q` exits 0
  </acceptance_criteria>
  <done>Simulation persistence contract is available to order, fill, ledger, and exchange services.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/unit/test_simulation_models.py tests/unit/test_simulation_store.py -q` passes
- [ ] `pytest -q` passes
- [ ] `rg "class OrderIntent|class RiskDecision|class SimulatedFill" src/polymarket_quant/domain/simulation.py` finds all required domain contracts
- [ ] `rg "CREATE SCHEMA IF NOT EXISTS simulation|simulation.orders|simulation.fills|simulation.risk_decisions|views.simulation_pnl" src/polymarket_quant/storage/postgres_schema.sql` finds all required SQL objects
- [ ] `rg "class SimulationStore|def insert_fill|def insert_valuation_snapshot" src/polymarket_quant/storage/simulation_store.py` finds required store APIs
</verification>

<success_criteria>
- Simulation order/risk/fill/ledger models exist and validate invalid inputs
- PostgreSQL has isolated `simulation` tables and views
- Store methods can be unit-tested without live PostgreSQL
- Existing Phase 1 and Phase 2 persistence files remain compatible
</success_criteria>

<output>
After completion, create `.planning/phases/03-simulation-exchange-portfolio-ledger/03-01-SUMMARY.md`
</output>
