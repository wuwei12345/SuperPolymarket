---
phase: 03-simulation-exchange-portfolio-ledger
plan: 05
type: execute
wave: 4
depends_on: ["03-02", "03-03", "03-04"]
files_modified:
  - src/polymarket_quant/services/__init__.py
  - src/polymarket_quant/services/paper_exchange.py
  - tests/unit/test_paper_exchange.py
  - README.md
autonomous: true
requirements: [SIM-01, SIM-02, SIM-03, RISK-01]
user_setup:
  - "For live PostgreSQL smoke only: set DATABASE_URL=postgresql://localhost/polymarket_quant after Plan 03-01 has initialized schema."
must_haves:
  truths:
    - Paper exchange submission records risk decisions before order acceptance.
    - Rejected orders do not enter the accepted/open order book.
    - Accepted marketable-limit orders can produce fills and ledger updates.
    - Cancel and replace routes use the lifecycle service.
    - Public service outputs include order, risk, fill, ledger, and valuation state without live trading or wallet/auth flows.
  artifacts:
    - `paper_exchange.py` orchestrates risk, lifecycle, fill, store, and ledger services.
    - `test_paper_exchange.py` covers allow, reject, partial fill, cancel, replace, and valuation flow.
    - README documents the Phase 3 paper exchange scope and explicitly says live trading is out of scope.
  key_links:
    - Phase 4 strategy runtime can call `PaperExchangeService.submit_order_intent`.
    - Phase 5 operator console can read store/query outputs produced by this service.
---

<objective>
Wire the Phase 3 services into a usable paper exchange boundary: submit order intent, run structured risk checks, create order lifecycle records, simulate fills, update ledger facts, compute valuation, and document the P0 scope.

Purpose: Downstream strategy and operator phases need one stable service boundary rather than calling risk, fill, and ledger helpers directly.
Output: `PaperExchangeService`, integration tests, exports, and README documentation.
</objective>

<execution_context>
@/Users/wuwei/Documents/polymarketQuantification/.codex/get-shit-done/workflows/execute-plan.md
@/Users/wuwei/Documents/polymarketQuantification/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-simulation-exchange-portfolio-ledger/03-CONTEXT.md
@.planning/phases/03-simulation-exchange-portfolio-ledger/03-RESEARCH.md
@.planning/phases/03-simulation-exchange-portfolio-ledger/03-PATTERNS.md
@.planning/phases/03-simulation-exchange-portfolio-ledger/03-02-SUMMARY.md
@.planning/phases/03-simulation-exchange-portfolio-ledger/03-03-SUMMARY.md
@.planning/phases/03-simulation-exchange-portfolio-ledger/03-04-SUMMARY.md
</context>

<threat_model>
## Threat Model

| ID | Threat | Severity | Mitigation Required In This Plan |
|----|--------|----------|-----------------------------------|
| T-03-15 | Exchange accepts orders before recording risk decisions | high | `submit_order_intent` must insert/return `RiskDecision` before order acceptance |
| T-03-16 | Rejected orders still create fills or ledger entries | high | Tests assert rejected submissions have no fills and no ledger updates |
| T-03-17 | Paper service accidentally introduces live trading/auth/wallet flow | high | README and tests assert no `ClobClient` trading/auth methods or wallet copy in `paper_exchange.py` |
| T-03-18 | Replace/cancel bypass order lifecycle validation | medium | Cancel and replace methods must delegate to `OrderLifecycleService` |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Implement PaperExchangeService submission orchestration</name>
  <files>src/polymarket_quant/services/__init__.py, src/polymarket_quant/services/paper_exchange.py, tests/unit/test_paper_exchange.py</files>
  <read_first>src/polymarket_quant/domain/simulation.py, src/polymarket_quant/services/order_risk.py, src/polymarket_quant/services/order_lifecycle.py, src/polymarket_quant/services/fill_engine.py, src/polymarket_quant/services/portfolio_ledger.py, src/polymarket_quant/storage/simulation_store.py</read_first>
  <action>Create `PaperExchangeService` with constructor accepting `store`, `risk_engine`, `lifecycle`, `fill_engine`, `ledger`, and `market_data_reader`. Define result model `PaperOrderResult` with `order`, `risk_decision`, `fills`, `ledger_updates`, and `valuation_snapshot`. Implement `submit_order_intent(intent: OrderIntent, constraints: MarketConstraints, limits: RiskLimits, snapshot: BookSnapshot | None = None, now: datetime | None = None) -> PaperOrderResult`. It must call `apply_risk_checks` first, persist or return the `RiskDecision`, reject without fills when decision is `REJECT`, create and accept/open a `SimulatedOrder` when allowed or warned, call `simulate_fills` when `snapshot` is provided, apply fills through `PortfolioLedgerService`, and upsert order/fill/ledger records through `SimulationStore` when a store is present. Export `PaperExchangeService` from `services/__init__.py`.</action>
  <verify>pytest tests/unit/test_paper_exchange.py -q</verify>
  <acceptance_criteria>
    - `src/polymarket_quant/services/paper_exchange.py` contains `class PaperExchangeService`
    - `src/polymarket_quant/services/paper_exchange.py` contains `class PaperOrderResult`
    - `src/polymarket_quant/services/paper_exchange.py` contains `def submit_order_intent`
    - `src/polymarket_quant/services/paper_exchange.py` contains `apply_risk_checks`
    - `src/polymarket_quant/services/paper_exchange.py` contains `simulate_fills`
    - `tests/unit/test_paper_exchange.py` contains `test_submit_records_risk_decision_before_accepting_order`
    - `tests/unit/test_paper_exchange.py` contains `test_rejected_order_has_no_fills_or_ledger_updates`
  </acceptance_criteria>
  <done>Paper exchange has a stable submission boundary with risk-first behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Add cancel and replace orchestration</name>
  <files>src/polymarket_quant/services/paper_exchange.py, tests/unit/test_paper_exchange.py</files>
  <read_first>src/polymarket_quant/services/paper_exchange.py, src/polymarket_quant/services/order_lifecycle.py, src/polymarket_quant/domain/simulation.py</read_first>
  <action>Add `cancel_order(client_order_id: str, now: datetime | None = None) -> PaperOrderResult` and `replace_order(client_order_id: str, replacement: OrderIntent, constraints: MarketConstraints, limits: RiskLimits, snapshot: BookSnapshot | None = None, now: datetime | None = None) -> PaperOrderResult`. `cancel_order` must load or accept an existing order object from the store/fake store, call `OrderLifecycleService.request_cancel`, then `OrderLifecycleService.cancel`, and persist transitions. `replace_order` must call `request_replace` on the original order, then submit the replacement intent through the same risk-first path as `submit_order_intent`. Do not implement GTD expiry or `post_only` enforcement here; leave comments or docs marking them P1 only if needed.</action>
  <verify>pytest tests/unit/test_paper_exchange.py -q</verify>
  <acceptance_criteria>
    - `src/polymarket_quant/services/paper_exchange.py` contains `def cancel_order`
    - `src/polymarket_quant/services/paper_exchange.py` contains `def replace_order`
    - `src/polymarket_quant/services/paper_exchange.py` contains `request_cancel`
    - `src/polymarket_quant/services/paper_exchange.py` contains `request_replace`
    - `tests/unit/test_paper_exchange.py` contains `test_cancel_order_uses_lifecycle_transitions`
    - `tests/unit/test_paper_exchange.py` contains `test_replace_order_runs_risk_checks_for_replacement`
  </acceptance_criteria>
  <done>Cancel and replace use the lifecycle and risk services instead of ad hoc state mutation.</done>
</task>

<task type="auto">
  <name>Task 3: Document Phase 3 P0 paper exchange scope</name>
  <files>README.md, tests/unit/test_paper_exchange.py</files>
  <read_first>README.md, .planning/phases/03-simulation-exchange-portfolio-ledger/03-CONTEXT.md, src/polymarket_quant/services/paper_exchange.py</read_first>
  <action>Add README section `## Phase 3 Paper Exchange`. State that Phase 3 supports `OrderIntent`, limit orders, marketable limit orders, cancel/replace, `client_order_id`, depth-driven paper fills, partial fills, tick-size and minimum-size checks, submit/cancel latency, cash/position/fill/fee ledger, conservative mark, realized/unrealized PnL, and structured `RiskDecision`. Add subsection `### Deferred beyond P0` listing `GTD expiry`, `post_only enforcement`, `portfolio-level hard risk`, `reward estimate`, `market/FOK/FAK orders`, `builder fees`, and `high-fidelity tick-by-tick replay fills`. State exactly `Phase 3 does not place live orders or use wallet authentication.` Add tests reading README to assert these strings.</action>
  <verify>pytest tests/unit/test_paper_exchange.py -q</verify>
  <acceptance_criteria>
    - `README.md` contains `## Phase 3 Paper Exchange`
    - `README.md` contains `OrderIntent`
    - `README.md` contains `marketable limit`
    - `README.md` contains `RiskDecision`
    - `README.md` contains `Deferred beyond P0`
    - `README.md` contains `Phase 3 does not place live orders or use wallet authentication.`
    - `tests/unit/test_paper_exchange.py` contains `test_readme_documents_phase3_paper_exchange_scope`
  </acceptance_criteria>
  <done>Phase 3 public documentation matches the locked P0/P1/P2 scope.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/unit/test_paper_exchange.py -q` passes
- [ ] `pytest -q` passes
- [ ] `rg "PaperExchangeService|submit_order_intent|cancel_order|replace_order" src/polymarket_quant/services/paper_exchange.py` finds required service APIs
- [ ] `rg "Phase 3 Paper Exchange|Deferred beyond P0|does not place live orders" README.md` finds required documentation
- [ ] `rg "wallet|auth|private_key|place_order" src/polymarket_quant/services/paper_exchange.py` does not find live-trading flow
</verification>

<success_criteria>
- Paper exchange submission runs risk before order acceptance
- Rejected orders do not create fills or ledger entries
- Accepted marketable-limit orders can produce fills and ledger updates
- Cancel and replace delegate to lifecycle service
- README documents P0 scope, deferred items, and no-live-trading boundary
</success_criteria>

<output>
After completion, create `.planning/phases/03-simulation-exchange-portfolio-ledger/03-05-SUMMARY.md`
</output>
