---
phase: 05-operator-console-safety-controls
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/polymarket_quant/domain/operator.py
  - src/polymarket_quant/services/operator_runtime_registry.py
  - src/polymarket_quant/services/realtime_strategy_runner.py
  - src/polymarket_quant/services/strategy_cli.py
  - tests/unit/test_operator_models.py
autonomous: true
requirements: [OPS-01, OPS-03]
user_setup: []
must_haves:
  truths:
    - Operator-facing runtime state must be explicit, not inferred from completed run artifacts.
    - The system must distinguish global mode from per-strategy/per-run mode.
    - Strategy status and last heartbeat need a shared registry/read model before the console UI can be honest.
  artifacts:
    - `operator.py` defines Phase 5 operator domain/state contracts.
    - `operator_runtime_registry.py` tracks global mode, per-run snapshots, and heartbeats.
    - Runtime entry paths publish state transitions into the registry.
  key_links:
    - Phase 5 overview table depends on truthful `state`, `new order status`, and `last heartbeat` fields.
    - Phase 5 mode switching cannot exist cleanly without explicit global-mode state.
---

<objective>
Create the runtime state foundation for the operator console: explicit operator domain models, a runtime registry, and heartbeat/status publication from the existing Phase 4 runtime entry points.

Purpose: The console cannot safely display `running / paused / error / blocked / last heartbeat` by guessing from artifacts written after a run completes.
Output: operator state models, registry service, runtime integration hooks, and tests.
</objective>

<execution_context>
@/Users/wuwei/Documents/polymarketQuantification/.codex/get-shit-done/workflows/execute-plan.md
@/Users/wuwei/Documents/polymarketQuantification/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-operator-console-safety-controls/05-CONTEXT.md
@.planning/phases/05-operator-console-safety-controls/05-RESEARCH.md
@.planning/phases/05-operator-console-safety-controls/05-PATTERNS.md
@.planning/phases/04-strategy-research-workbench/04-CONTEXT.md
@src/polymarket_quant/services/strategy_cli.py
@src/polymarket_quant/services/realtime_strategy_runner.py
</context>

<threat_model>
## Threat Model

| ID | Threat | Severity | Mitigation Required In This Plan |
|----|--------|----------|-----------------------------------|
| T-05-01 | Console shows stale or fabricated strategy state because no runtime heartbeat exists | high | Add explicit heartbeat and run-state registry written by runtime paths |
| T-05-02 | Global mode and local run mode are conflated, making safety semantics ambiguous | high | Model the two mode layers separately in domain/state contracts |
| T-05-03 | Runtime-state tracking becomes UI-only glue with no typed contract | medium | Introduce dedicated operator domain models and unit tests |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Define operator state domain models</name>
  <files>src/polymarket_quant/domain/operator.py, tests/unit/test_operator_models.py</files>
  <read_first>.planning/phases/05-operator-console-safety-controls/05-CONTEXT.md, src/polymarket_quant/domain/strategy.py, src/polymarket_quant/domain/simulation.py</read_first>
  <action>Create `src/polymarket_quant/domain/operator.py` with typed models/enums for operator-facing state. Include separate global mode vs local run mode semantics, connection component state, strategy/run status snapshot, alert severity, new-order block state, and runtime heartbeat shape. Keep these models strict (`extra="forbid"`) and ready for service/UI consumption.</action>
  <verify>pytest tests/unit/test_operator_models.py -q</verify>
  <acceptance_criteria>
    - `src/polymarket_quant/domain/operator.py` contains `global_mode`
    - `src/polymarket_quant/domain/operator.py` contains `last_heartbeat`
    - `src/polymarket_quant/domain/operator.py` contains `Critical`
    - `tests/unit/test_operator_models.py` contains `test_operator_models_distinguish_global_and_local_modes`
    - `tests/unit/test_operator_models.py` contains `test_runtime_snapshot_tracks_last_heartbeat_and_block_state`
  </acceptance_criteria>
  <done>Phase 5 has a typed operator-state contract that downstream services can reuse.</done>
</task>

<task type="auto">
  <name>Task 2: Implement runtime registry and heartbeat publication</name>
  <files>src/polymarket_quant/services/operator_runtime_registry.py, src/polymarket_quant/services/strategy_cli.py, src/polymarket_quant/services/realtime_strategy_runner.py, tests/unit/test_operator_models.py</files>
  <read_first>src/polymarket_quant/domain/operator.py, src/polymarket_quant/services/strategy_cli.py, src/polymarket_quant/services/realtime_strategy_runner.py</read_first>
  <action>Create `OperatorRuntimeRegistry` to track global mode, current per-run/per-strategy snapshots, and heartbeat timestamps. Add lightweight integration hooks so `StrategyCliService` and realtime execution paths can publish started/running/finished/error snapshots plus local run mode and block state. Keep the registry dependency-injectable so tests can use an in-memory instance.</action>
  <verify>pytest tests/unit/test_operator_models.py -q</verify>
  <acceptance_criteria>
    - `src/polymarket_quant/services/operator_runtime_registry.py` contains `class OperatorRuntimeRegistry`
    - `src/polymarket_quant/services/operator_runtime_registry.py` contains `set_global_mode`
    - `src/polymarket_quant/services/operator_runtime_registry.py` contains `heartbeat`
    - `src/polymarket_quant/services/strategy_cli.py` contains `runtime_registry`
    - `tests/unit/test_operator_models.py` contains `test_registry_tracks_global_mode_and_run_heartbeat`
    - `tests/unit/test_operator_models.py` contains `test_strategy_cli_publishes_runtime_snapshots`
  </acceptance_criteria>
  <done>The runtime has a truthful operator-state source for the Phase 5 console.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/unit/test_operator_models.py -q` passes
- [ ] `pytest -q` passes
- [ ] `rg "global_mode|last_heartbeat|blocked" src/polymarket_quant/domain/operator.py src/polymarket_quant/services/operator_runtime_registry.py` finds required operator state fields
- [ ] `rg "runtime_registry|heartbeat" src/polymarket_quant/services/strategy_cli.py src/polymarket_quant/services/realtime_strategy_runner.py` finds runtime publication hooks
</verification>

<success_criteria>
- Operator state is typed and explicit
- Global mode and local run mode are separate
- Strategy runtime publishes heartbeat/status updates into a registry
- Later Phase 5 plans can read truthful current state instead of guessing from artifacts
</success_criteria>

<output>
After completion, create `.planning/phases/05-operator-console-safety-controls/05-01-SUMMARY.md`
</output>
