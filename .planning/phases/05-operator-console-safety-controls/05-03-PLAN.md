---
phase: 05-operator-console-safety-controls
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - src/polymarket_quant/services/operator_queries.py
  - tests/unit/test_operator_queries.py
autonomous: true
requirements: [OPS-01, RISK-03]
user_setup: []
must_haves:
  truths:
    - The console should read from a dedicated query layer, not from ad hoc store calls inside Streamlit.
    - Grouped operator views must support `strategy`, `market`, `event`, and `time window`.
    - The default overview remains strategy-first, but market/event/time-window rollups must still exist.
    - Runs/artifacts, positions/orders, pnl/exposure, and alert timeline should come from one coherent read model.
  artifacts:
    - `operator_queries.py` exposes grouped overview, detail panes, timeline, and run/artifact readers.
    - `test_operator_queries.py` covers grouping, filter propagation, and joined operator state.
  key_links:
    - The homepage and secondary views both depend on these operator queries.
    - Phase 5 needs grouped risk/performance analytics even though Phase 4 metrics were run-centric.
---

<objective>
Build the read-side query layer for the operator console: merge runtime registry state, simulation facts, market-data health, run manifests/artifacts, and grouped analytics into homepage-ready datasets.

Purpose: Streamlit should render a coherent operator view, not embed multi-source aggregation logic directly in page code.
Output: operator query service and tests.
</objective>

<execution_context>
@/Users/wuwei/Documents/polymarketQuantification/.codex/get-shit-done/workflows/execute-plan.md
@/Users/wuwei/Documents/polymarketQuantification/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-operator-console-safety-controls/05-CONTEXT.md
@.planning/phases/05-operator-console-safety-controls/05-RESEARCH.md
@.planning/phases/05-operator-console-safety-controls/05-PATTERNS.md
@src/polymarket_quant/services/experiment_metrics.py
@src/polymarket_quant/services/run_artifacts.py
@src/polymarket_quant/storage/market_data_store.py
@src/polymarket_quant/storage/simulation_store.py
</context>

<threat_model>
## Threat Model

| ID | Threat | Severity | Mitigation Required In This Plan |
|----|--------|----------|-----------------------------------|
| T-05-07 | Console data is inconsistent because each widget reads different raw sources independently | high | Create one operator query layer that joins runtime state, market data, and run artifacts |
| T-05-08 | Grouped analytics support only strategy and silently omit market/event/time-window views | medium | Explicitly implement all required grouping axes and test them |
| T-05-09 | Runs/artifacts and alert timelines are bolted on as separate readers with incompatible filter semantics | medium | Expose shared-filter-aware query methods from one service |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Implement operator read models and grouped overview queries</name>
  <files>src/polymarket_quant/services/operator_queries.py, tests/unit/test_operator_queries.py</files>
  <read_first>.planning/phases/05-operator-console-safety-controls/05-CONTEXT.md, src/polymarket_quant/services/experiment_metrics.py, src/polymarket_quant/storage/simulation_store.py</read_first>
  <action>Create `OperatorQueryService` that can build grouped operator datasets by `strategy`, `market`, `event`, and `time_window`. It should merge runtime registry snapshots with run metrics, positions, orders, and alert summaries into overview rows. Include grouped analytics for PnL, drawdown, exposure, turnover, and win rate, even if grouped win rate must be derived from fills or grouped outcomes rather than Phase 4 run summaries alone.</action>
  <verify>pytest tests/unit/test_operator_queries.py -q</verify>
  <acceptance_criteria>
    - `src/polymarket_quant/services/operator_queries.py` contains `strategy`
    - `src/polymarket_quant/services/operator_queries.py` contains `market`
    - `src/polymarket_quant/services/operator_queries.py` contains `event`
    - `src/polymarket_quant/services/operator_queries.py` contains `time_window`
    - `src/polymarket_quant/services/operator_queries.py` contains `win_rate`
    - `tests/unit/test_operator_queries.py` contains `test_operator_queries_group_by_strategy_market_event_and_time_window`
  </acceptance_criteria>
  <done>Phase 5 has one coherent grouped query layer for operator analytics.</done>
</task>

<task type="auto">
  <name>Task 2: Add detail-pane, timeline, and run-artifact queries</name>
  <files>src/polymarket_quant/services/operator_queries.py, tests/unit/test_operator_queries.py</files>
  <read_first>src/polymarket_quant/services/run_artifacts.py, src/polymarket_quant/services/operator_runtime_registry.py, src/polymarket_quant/storage/market_data_store.py</read_first>
  <action>Extend the query service with shared-filter-aware methods for:
  - strategy overview rows
  - `Positions / Orders`
  - `PnL / Exposure`
  - alert/event timeline
  - runs/artifacts listing
  These methods should read manifests and parquet artifacts where appropriate, incorporate latest market/connection health, and preserve one consistent filter vocabulary across datasets.</action>
  <verify>pytest tests/unit/test_operator_queries.py -q</verify>
  <acceptance_criteria>
    - `src/polymarket_quant/services/operator_queries.py` contains `last_heartbeat`
    - `src/polymarket_quant/services/operator_queries.py` contains `alerts_timeline`
    - `src/polymarket_quant/services/operator_queries.py` contains `runs_artifacts`
    - `tests/unit/test_operator_queries.py` contains `test_operator_queries_merge_run_metrics_positions_orders_and_alerts`
    - `tests/unit/test_operator_queries.py` contains `test_operator_queries_list_runs_and_artifacts_from_manifest_bundle`
  </acceptance_criteria>
  <done>The operator console can render all Phase 5 sections from one read model.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/unit/test_operator_queries.py -q` passes
- [ ] `pytest -q` passes
- [ ] `rg "strategy|market|event|time_window|win_rate" src/polymarket_quant/services/operator_queries.py` finds grouped analytics support
- [ ] `rg "last_heartbeat|alerts_timeline|runs_artifacts" src/polymarket_quant/services/operator_queries.py` finds homepage/secondary view query methods
</verification>

<success_criteria>
- One query layer powers all major console surfaces
- Required grouping axes are supported
- Homepage and secondary views share one filter vocabulary
- Runs/artifacts and alerts timeline are compatible with the same operator state model
</success_criteria>

<output>
After completion, create `.planning/phases/05-operator-console-safety-controls/05-03-SUMMARY.md`
</output>
