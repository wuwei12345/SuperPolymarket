---
phase: 05-operator-console-safety-controls
plan: 04
type: execute
wave: 4
depends_on: ["05-03"]
files_modified:
  - src/polymarket_quant/ui/contracts.py
  - src/polymarket_quant/ui/operator_console_app.py
  - tests/unit/test_operator_console_ui_contract.py
autonomous: true
requirements: [OPS-01, RISK-03]
user_setup: []
must_haves:
  truths:
    - The homepage must be single-page sectional, not tab-first.
    - The top status band answers “is anything broken or dangerous?” before deeper analytics.
    - Shared filters drive the overview table, detail panes, and timeline together.
    - The default detail panes are only `Positions / Orders` and `PnL / Exposure`.
  artifacts:
    - `contracts.py` locks the Phase 5 UI contract.
    - `operator_console_app.py` renders the status band, strategy overview, detail toggles, and event timeline.
    - `test_operator_console_ui_contract.py` asserts layout/copy decisions and out-of-scope exclusions.
  key_links:
    - The console layout must extend earlier Streamlit monitor patterns without turning into a generic tabbed dashboard.
    - Safety and grouped analytics from prior plans must already be consumable by the UI.
---

<objective>
Implement the main Phase 5 Streamlit operator console homepage with the locked single-page layout, fixed status band, shared filters, strategy-first overview, read-only timeline, and two default detail blocks.

Purpose: This is the operator-facing surface for the phase; the user must be able to scan health and risk before drilling into details.
Output: UI contract constants, Streamlit page, and UI contract tests.
</objective>

<execution_context>
@/Users/wuwei/Documents/polymarketQuantification/.codex/get-shit-done/workflows/execute-plan.md
@/Users/wuwei/Documents/polymarketQuantification/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-operator-console-safety-controls/05-CONTEXT.md
@.planning/phases/05-operator-console-safety-controls/05-RESEARCH.md
@.planning/phases/05-operator-console-safety-controls/05-PATTERNS.md
@src/polymarket_quant/ui/contracts.py
@src/polymarket_quant/ui/market_universe_app.py
@src/polymarket_quant/ui/market_data_app.py
@src/polymarket_quant/services/operator_queries.py
</context>

<threat_model>
## Threat Model

| ID | Threat | Severity | Mitigation Required In This Plan |
|----|--------|----------|-----------------------------------|
| T-05-10 | UI drifts into a generic tabbed dashboard and loses the locked operator hierarchy | high | Lock layout constants and add tests asserting single-page sectional structure |
| T-05-11 | Shared filters become local widget state, causing mismatched tables/timeline | medium | Centralize filter helpers and propagate one filter set into all rendered sections |
| T-05-12 | Homepage becomes overloaded with extra panes beyond the locked defaults | medium | Keep only the approved detail blocks on the homepage and push runs/artifacts later |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Define the Phase 5 UI contract</name>
  <files>src/polymarket_quant/ui/contracts.py, tests/unit/test_operator_console_ui_contract.py</files>
  <read_first>.planning/phases/05-operator-console-safety-controls/05-CONTEXT.md, src/polymarket_quant/ui/contracts.py, tests/unit/test_market_universe_ui_contract.py, tests/unit/test_market_data_ui_contract.py</read_first>
  <action>Append Phase 5 operator-console constants to `contracts.py`. Lock the page title, status-band fields, shared filters, strategy overview columns, detail-pane labels, severity labels, and layout regions. Encode that the main structure is single-page with filters on the left, overview in the main area, timeline at the bottom, and only two default detail panes.</action>
  <verify>pytest tests/unit/test_operator_console_ui_contract.py -q</verify>
  <acceptance_criteria>
    - `src/polymarket_quant/ui/contracts.py` contains `Operator Console`
    - `src/polymarket_quant/ui/contracts.py` contains `last heartbeat`
    - `src/polymarket_quant/ui/contracts.py` contains `Positions / Orders`
    - `src/polymarket_quant/ui/contracts.py` contains `PnL / Exposure`
    - `tests/unit/test_operator_console_ui_contract.py` contains `test_operator_console_contract_matches_phase5_context`
  </acceptance_criteria>
  <done>The locked Phase 5 layout and copy contract is encoded in testable constants.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the Streamlit operator console homepage</name>
  <files>src/polymarket_quant/ui/operator_console_app.py, tests/unit/test_operator_console_ui_contract.py</files>
  <read_first>src/polymarket_quant/ui/contracts.py, src/polymarket_quant/services/operator_queries.py, src/polymarket_quant/ui/market_universe_app.py, src/polymarket_quant/ui/market_data_app.py</read_first>
  <action>Create `operator_console_app.py` with `main()` plus helper functions for status-band rendering, shared filters, overview dataframe construction, detail toggles, and alert timeline rendering. Use Streamlit and keep the page single-page sectional. The status band should be fixed at the top, the overview should default to strategy aggregation, the lower section should render recent event flow, and the homepage should not use tabs as its primary structure.</action>
  <verify>pytest tests/unit/test_operator_console_ui_contract.py -q</verify>
  <acceptance_criteria>
    - `src/polymarket_quant/ui/operator_console_app.py` contains `def main(`
    - `src/polymarket_quant/ui/operator_console_app.py` contains `st.sidebar`
    - `src/polymarket_quant/ui/operator_console_app.py` contains `last heartbeat`
    - `src/polymarket_quant/ui/operator_console_app.py` contains `Critical`
    - `src/polymarket_quant/ui/operator_console_app.py` does not contain `st.tabs(`
    - `tests/unit/test_operator_console_ui_contract.py` contains `test_operator_console_is_single_page_not_tab_first`
  </acceptance_criteria>
  <done>The homepage renders the locked Phase 5 operator structure without slipping into a tabbed dashboard.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/unit/test_operator_console_ui_contract.py -q` passes
- [ ] `pytest -q` passes
- [ ] `rg "Operator Console|last heartbeat|Positions / Orders|PnL / Exposure" src/polymarket_quant/ui/contracts.py src/polymarket_quant/ui/operator_console_app.py` finds required UI copy
- [ ] `rg "st.tabs\\(" src/polymarket_quant/ui/operator_console_app.py` does not find tab-first layout usage
</verification>

<success_criteria>
- Status band is top-priority and fixed in the page hierarchy
- Filters are shared across the console
- Strategy-first overview and read-only timeline are rendered
- Homepage includes only the two approved detail panes
</success_criteria>

<output>
After completion, create `.planning/phases/05-operator-console-safety-controls/05-04-SUMMARY.md`
</output>
