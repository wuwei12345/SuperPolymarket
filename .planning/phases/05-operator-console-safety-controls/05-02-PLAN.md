---
phase: 05-operator-console-safety-controls
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/polymarket_quant/services/operator_safety.py
  - tests/unit/test_operator_safety.py
autonomous: true
requirements: [RISK-02, OPS-03]
user_setup: []
must_haves:
  truths:
    - Safety rules must be conservative and fail-safe by default.
    - Expiry and liquidity logic produce both alerts and new-order gate decisions.
    - Critical expiry blocks only new exposure and still allows reduce/close/risk-release behavior.
    - Mode switching must run through a preflight check before the UI can confirm anything.
  artifacts:
    - `operator_safety.py` derives alert severities and new-order block state.
    - `operator_safety.py` also exposes mode-preflight evaluation.
    - `test_operator_safety.py` covers threshold escalation, block semantics, and preflight results.
  key_links:
    - Console status cards and timeline depend on these alert/block decisions.
    - Mode-switch UI in later plans must consume preflight results instead of inventing rules.
---

<objective>
Implement the Phase 5 safety-control engine: expiry, liquidity, connection, and mode checks that derive `Info / Warning / Critical` alerts, new-order block state, and preflight blockers for mode changes.

Purpose: The operator console must surface clear guardrails and block new exposure conservatively without collapsing risk-release actions.
Output: safety service, preflight evaluation, and deterministic tests.
</objective>

<execution_context>
@/Users/wuwei/Documents/polymarketQuantification/.codex/get-shit-done/workflows/execute-plan.md
@/Users/wuwei/Documents/polymarketQuantification/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-operator-console-safety-controls/05-CONTEXT.md
@.planning/phases/05-operator-console-safety-controls/05-RESEARCH.md
@.planning/phases/05-operator-console-safety-controls/05-PATTERNS.md
@.planning/phases/03-simulation-exchange-portfolio-ledger/03-CONTEXT.md
@src/polymarket_quant/services/order_risk.py
@src/polymarket_quant/domain/market_data.py
</context>

<threat_model>
## Threat Model

| ID | Threat | Severity | Mitigation Required In This Plan |
|----|--------|----------|-----------------------------------|
| T-05-04 | Critical expiry/liquidity conditions are only displayed and do not actually block new exposure | high | Emit explicit new-order gate decisions from safety evaluation |
| T-05-05 | Safety service blocks all actions, including risk-release orders, near expiry | high | Distinguish exposure-increasing actions from reduce/close/risk-release actions |
| T-05-06 | Mode switches bypass conservative checks and confirmation gating | high | Add a dedicated preflight result model and service |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Implement expiry and liquidity alert/block evaluation</name>
  <files>src/polymarket_quant/services/operator_safety.py, tests/unit/test_operator_safety.py</files>
  <read_first>.planning/phases/05-operator-console-safety-controls/05-CONTEXT.md, src/polymarket_quant/domain/operator.py, src/polymarket_quant/domain/market_data.py</read_first>
  <action>Create a safety service that evaluates expiry, spread, top-of-book depth, recent activity, gap-fill instability, book completeness, connection completeness, and mode availability into structured alerts and new-order block state. Encode the locked thresholds exactly: expiry at 120m/60m/15m, warning liquidity at `max(3 ticks, 3%)` and critical liquidity at `max(5 ticks, 5%)`, plus depth/activity/gap/book checks. Distinguish `Info`, `Warning`, and `Critical` outputs and surface `allowed / partially blocked / fully blocked` new-order state.</action>
  <verify>pytest tests/unit/test_operator_safety.py -q</verify>
  <acceptance_criteria>
    - `src/polymarket_quant/services/operator_safety.py` contains `Info`
    - `src/polymarket_quant/services/operator_safety.py` contains `Warning`
    - `src/polymarket_quant/services/operator_safety.py` contains `Critical`
    - `src/polymarket_quant/services/operator_safety.py` contains `top_of_book_depth_usdc`
    - `src/polymarket_quant/services/operator_safety.py` contains `gap_fill`
    - `tests/unit/test_operator_safety.py` contains `test_expiry_thresholds_escalate_by_minutes`
    - `tests/unit/test_operator_safety.py` contains `test_liquidity_thresholds_respect_ticks_depth_activity_and_gap_fill`
  </acceptance_criteria>
  <done>Phase 5 can derive conservative alert and new-order block state from factual market/runtime conditions.</done>
</task>

<task type="auto">
  <name>Task 2: Implement action-aware gate semantics and mode preflight</name>
  <files>src/polymarket_quant/services/operator_safety.py, tests/unit/test_operator_safety.py</files>
  <read_first>src/polymarket_quant/services/operator_safety.py, .planning/phases/05-operator-console-safety-controls/05-CONTEXT.md</read_first>
  <action>Add action-aware gating so critical expiry/liquidity states block only exposure-increasing actions while still allowing `reduce`, `close`, and risk-release orders. Add mode-preflight evaluation that returns blocking and warning reasons before any switch between global modes is confirmed. Keep `live-disabled` as a protective mode boundary, not as a live execution path.</action>
  <verify>pytest tests/unit/test_operator_safety.py -q</verify>
  <acceptance_criteria>
    - `src/polymarket_quant/services/operator_safety.py` contains `increase_exposure`
    - `src/polymarket_quant/services/operator_safety.py` contains `reduce`
    - `src/polymarket_quant/services/operator_safety.py` contains `preflight`
    - `src/polymarket_quant/services/operator_safety.py` contains `live-disabled`
    - `tests/unit/test_operator_safety.py` contains `test_critical_expiry_blocks_only_increasing_exposure`
    - `tests/unit/test_operator_safety.py` contains `test_mode_preflight_reports_blockers_before_switch`
  </acceptance_criteria>
  <done>Safety rules are conservative without preventing explicit risk-release behavior, and mode switching gains a typed preflight boundary.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/unit/test_operator_safety.py -q` passes
- [ ] `pytest -q` passes
- [ ] `rg "120|60|15|max\\(3 ticks, 3%\\)|max\\(5 ticks, 5%\\)|gap_fill" src/polymarket_quant/services/operator_safety.py` finds threshold logic
- [ ] `rg "preflight|increase_exposure|live-disabled" src/polymarket_quant/services/operator_safety.py` finds gate/preflight semantics
</verification>

<success_criteria>
- Expiry and liquidity thresholds follow the locked Phase 5 contract
- Critical states block new exposure conservatively
- Reduce/close/risk-release actions remain allowed where required
- Mode switching gets a preflight decision object before UI confirmation
</success_criteria>

<output>
After completion, create `.planning/phases/05-operator-console-safety-controls/05-02-SUMMARY.md`
</output>
