---
phase: 04-strategy-research-workbench
plan: 04
type: execute
wave: 4
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - src/polymarket_quant/services/signal_execution.py
  - src/polymarket_quant/services/realtime_strategy_runner.py
  - tests/unit/test_signal_execution.py
autonomous: true
requirements: [STRAT-02, STRAT-03]
user_setup: []
must_haves:
  truths:
    - Target-based signals are translated by a dedicated sizing/execution layer, not by strategies themselves.
    - Realtime paper mode reuses the same strategy lifecycle and routes execution through `PaperExchangeService`.
    - Recent fills, order state, and risk decisions feed back into the shared context.
  artifacts:
    - `signal_execution.py` converts target signals into execution decisions.
    - `realtime_strategy_runner.py` runs strategies in realtime paper mode using the shared runtime core.
    - `test_signal_execution.py` covers target translation and paper-exchange routing.
  key_links:
    - This is the bridge between Phase 4 strategy runtime and the verified Phase 3 execution layer.
    - CLI plan 04-05 depends on this runtime mode existing.
---

<objective>
Implement the Phase 4 signal-to-execution adapter and realtime paper runner so strategies can use one contract while execution still flows through sizing policy and `PaperExchangeService`.

Purpose: The replay runtime is not enough; Phase 4 must prove the same strategy logic can drive realtime paper mode without collapsing abstractions.
Output: target-signal execution adapter, realtime paper runner, and tests.
</objective>

<execution_context>
@/Users/wuwei/Documents/polymarketQuantification/.codex/get-shit-done/workflows/execute-plan.md
@/Users/wuwei/Documents/polymarketQuantification/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-strategy-research-workbench/04-CONTEXT.md
@.planning/phases/04-strategy-research-workbench/04-RESEARCH.md
@.planning/phases/04-strategy-research-workbench/04-PATTERNS.md
@.planning/phases/04-strategy-research-workbench/04-VALIDATION.md
@.planning/phases/04-strategy-research-workbench/04-03-PLAN.md
@.planning/phases/03-simulation-exchange-portfolio-ledger/03-CONTEXT.md
@.planning/phases/03-simulation-exchange-portfolio-ledger/03-VERIFICATION.md
@src/polymarket_quant/services/paper_exchange.py
</context>

<threat_model>
## Threat Model

| ID | Threat | Severity | Mitigation Required In This Plan |
|----|--------|----------|-----------------------------------|
| T-04-10 | Strategies emit direct orders, destroying the alpha/sizing/execution split | high | A dedicated adapter must translate target signals into execution decisions and `OrderIntent` |
| T-04-11 | Realtime paper mode bypasses the verified Phase 3 execution boundary | high | Runner must route execution through `PaperExchangeService` only |
| T-04-12 | Context feedback misses fills/risk/order state, making adaptive strategies inconsistent | medium | Execution feedback must be written back into runtime context for later callbacks |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Implement target-based signal translation</name>
  <files>src/polymarket_quant/services/signal_execution.py, tests/unit/test_signal_execution.py</files>
  <read_first>src/polymarket_quant/domain/strategy.py, src/polymarket_quant/domain/simulation.py, src/polymarket_quant/services/paper_exchange.py</read_first>
  <action>Create a service that converts `StrategySignal` into sized execution intents while preserving the separation between alpha, sizing, and execution. It should compare target exposure/position against current holdings, derive desired deltas, and produce `OrderIntent` inputs suitable for `PaperExchangeService`. Keep exact sizing policy pluggable, but ensure tests cover both `target_exposure` and fallback `target_position` flows.</action>
  <verify>pytest tests/unit/test_signal_execution.py -q</verify>
  <acceptance_criteria>
    - `src/polymarket_quant/services/signal_execution.py` contains `target_exposure`
    - `src/polymarket_quant/services/signal_execution.py` contains `target_position`
    - `src/polymarket_quant/services/signal_execution.py` contains `OrderIntent`
    - `tests/unit/test_signal_execution.py` contains `test_target_exposure_signal_translates_into_order_intent_delta`
    - `tests/unit/test_signal_execution.py` contains `test_target_position_fallback_is_supported`
  </acceptance_criteria>
  <done>Signal translation exists without forcing strategies to know execution semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Implement realtime paper strategy runner</name>
  <files>src/polymarket_quant/services/realtime_strategy_runner.py, tests/unit/test_signal_execution.py</files>
  <read_first>src/polymarket_quant/services/strategy_runtime.py, src/polymarket_quant/services/signal_execution.py, src/polymarket_quant/services/paper_exchange.py</read_first>
  <action>Create a realtime paper runner that reuses the shared runtime contract, consumes live-ish market events from an injected feed, sends translated intents to `PaperExchangeService`, and feeds resulting orders/fills/risk decisions back into strategy context. Keep feed/client dependencies injectable so tests can run with fake event sources and fake execution services.</action>
  <verify>pytest tests/unit/test_signal_execution.py -q</verify>
  <acceptance_criteria>
    - `src/polymarket_quant/services/realtime_strategy_runner.py` contains `PaperExchangeService`
    - `src/polymarket_quant/services/realtime_strategy_runner.py` contains `on_event`
    - `src/polymarket_quant/services/realtime_strategy_runner.py` contains `risk_decision` or equivalent feedback path
    - `tests/unit/test_signal_execution.py` contains `test_realtime_runner_routes_orders_through_paper_exchange`
    - `tests/unit/test_signal_execution.py` contains `test_execution_feedback_updates_runtime_context`
  </acceptance_criteria>
  <done>Realtime paper mode is wired to the same strategy lifecycle and Phase 3 execution boundary.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/unit/test_signal_execution.py -q` passes
- [ ] `pytest -q` passes
- [ ] `rg "PaperExchangeService|target_exposure|target_position" src/polymarket_quant/services/signal_execution.py src/polymarket_quant/services/realtime_strategy_runner.py` finds required bridge points
- [ ] `rg "test_target_exposure_signal_translates_into_order_intent_delta|test_realtime_runner_routes_orders_through_paper_exchange" tests/unit/test_signal_execution.py` finds required tests
</verification>

<success_criteria>
- Signals are translated outside strategy code
- Realtime paper mode reuses the shared strategy contract
- Execution still flows through the verified Phase 3 services
</success_criteria>

<output>
After completion, create `.planning/phases/04-strategy-research-workbench/04-04-SUMMARY.md`
</output>
