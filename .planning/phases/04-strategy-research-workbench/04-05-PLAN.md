---
phase: 04-strategy-research-workbench
plan: 05
type: execute
wave: 5
depends_on: ["04-02", "04-03", "04-04"]
files_modified:
  - README.md
  - src/polymarket_quant/services/__init__.py
  - src/polymarket_quant/services/experiment_metrics.py
  - src/polymarket_quant/services/strategy_cli.py
  - tests/unit/test_strategy_cli.py
autonomous: true
requirements: [STRAT-02, OPS-02, STRAT-03]
user_setup:
  - "If you want to smoke-test YAML configs locally after implementation, ensure editable dependencies are installed with the Phase 4 additions."
must_haves:
  truths:
    - CLI is the primary Phase 4 entry point.
    - Config resolution supports YAML/JSON and persists resolved values into the manifest.
    - Metrics and run summaries are computed from factual artifacts, not log scraping.
  artifacts:
    - `strategy_cli.py` exposes replay/realtime-paper/research entry points.
    - `experiment_metrics.py` computes the locked Phase 4 metric set.
    - README documents Phase 4 strategy runtime and artifact bundle usage.
  key_links:
    - This plan makes the Phase 4 runtime usable by a researcher, not just testable internally.
    - Phase 5 operator work can later read the same run directories and metrics.
---

<objective>
Add the Phase 4 user-facing runtime surface: CLI/config resolution, metrics computation, service exports, and README documentation for explainable reproducible runs.

Purpose: Phase 4 is complete only when a researcher can launch a run, inspect its manifest/artifacts, and compare outcomes from stable metrics.
Output: CLI, metrics service, docs, and tests.
</objective>

<execution_context>
@/Users/wuwei/Documents/polymarketQuantification/.codex/get-shit-done/workflows/execute-plan.md
@/Users/wuwei/Documents/polymarketQuantification/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-strategy-research-workbench/04-CONTEXT.md
@.planning/phases/04-strategy-research-workbench/04-RESEARCH.md
@.planning/phases/04-strategy-research-workbench/04-PATTERNS.md
@.planning/phases/04-strategy-research-workbench/04-VALIDATION.md
@.planning/phases/04-strategy-research-workbench/04-02-PLAN.md
@.planning/phases/04-strategy-research-workbench/04-03-PLAN.md
@.planning/phases/04-strategy-research-workbench/04-04-PLAN.md
@README.md
</context>

<threat_model>
## Threat Model

| ID | Threat | Severity | Mitigation Required In This Plan |
|----|--------|----------|-----------------------------------|
| T-04-13 | CLI runs are not reproducible because YAML/JSON configs are not fully resolved into the manifest | high | CLI must persist resolved config snapshots into run artifacts |
| T-04-14 | Metrics are inconsistent across modes because they are derived from logs or mode-specific shortcuts | high | One metrics service must compute summaries from factual execution/run artifacts |
| T-04-15 | README and exports misdescribe the runtime surface, causing later phases to integrate the wrong boundary | medium | README and `services/__init__.py` must reflect the final public Phase 4 surface |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Implement experiment metrics service</name>
  <files>src/polymarket_quant/services/experiment_metrics.py, tests/unit/test_strategy_cli.py</files>
  <read_first>src/polymarket_quant/domain/strategy.py, src/polymarket_quant/domain/simulation.py, .planning/phases/04-strategy-research-workbench/04-CONTEXT.md</read_first>
  <action>Create a metrics service that computes the locked metric set for a run: total return, realized/unrealized pnl, turnover, fill rate, cancel rate, average holding time, max drawdown, exposure peak, reject count, and slippage metrics. Inputs should be explicit factual records or artifact tables, not raw logs. Tests should cover at least one small synthetic run payload.</action>
  <verify>pytest tests/unit/test_strategy_cli.py -q</verify>
  <acceptance_criteria>
    - `src/polymarket_quant/services/experiment_metrics.py` contains `max_drawdown`
    - `src/polymarket_quant/services/experiment_metrics.py` contains `fill_rate`
    - `src/polymarket_quant/services/experiment_metrics.py` contains `slippage`
    - `tests/unit/test_strategy_cli.py` contains `test_metrics_service_computes_required_run_summary_fields`
  </acceptance_criteria>
  <done>Run summaries are factual and comparable across modes.</done>
</task>

<task type="auto">
  <name>Task 2: Implement CLI and resolved-config entry path</name>
  <files>src/polymarket_quant/services/strategy_cli.py, src/polymarket_quant/services/__init__.py, tests/unit/test_strategy_cli.py</files>
  <read_first>src/polymarket_quant/services/replay_runtime.py, src/polymarket_quant/services/realtime_strategy_runner.py, src/polymarket_quant/services/run_artifacts.py</read_first>
  <action>Create the primary CLI-facing service for Phase 4. It must load YAML/JSON config, resolve defaults into a `ResolvedRunConfig`, choose the requested mode (`replay`, `realtime_paper`, or `research`), create the run directory, invoke the appropriate runner, and persist the final manifest and metrics. Export the main services from `src/polymarket_quant/services/__init__.py`.</action>
  <verify>pytest tests/unit/test_strategy_cli.py -q</verify>
  <acceptance_criteria>
    - `src/polymarket_quant/services/strategy_cli.py` contains `yaml`
    - `src/polymarket_quant/services/strategy_cli.py` contains `json`
    - `src/polymarket_quant/services/strategy_cli.py` contains `replay`
    - `src/polymarket_quant/services/strategy_cli.py` contains `realtime_paper`
    - `tests/unit/test_strategy_cli.py` contains `test_cli_resolves_yaml_config_and_writes_manifest`
    - `tests/unit/test_strategy_cli.py` contains `test_cli_dispatches_to_selected_run_mode`
  </acceptance_criteria>
  <done>Researchers have a stable CLI/config entry point for Phase 4.</done>
</task>

<task type="auto">
  <name>Task 3: Document the Phase 4 runtime surface</name>
  <files>README.md, tests/unit/test_strategy_cli.py</files>
  <read_first>README.md, .planning/phases/04-strategy-research-workbench/04-CONTEXT.md, src/polymarket_quant/services/strategy_cli.py</read_first>
  <action>Add a README section for Phase 4 describing the strategy class lifecycle, target-based signals, replay/realtime paper modes, CLI-first workflow, YAML/JSON config, and run artifact directory structure. The docs must state that Phase 4 is about explainable, reproducible, comparable runs rather than merely running strategy code. Add a doc assertion test in `test_strategy_cli.py` for key Phase 4 scope strings.</action>
  <verify>pytest tests/unit/test_strategy_cli.py -q</verify>
  <acceptance_criteria>
    - `README.md` contains `## Phase 4 Strategy Runtime`
    - `README.md` contains `on_init`
    - `README.md` contains `target_exposure`
    - `README.md` contains `manifest.json`
    - `README.md` contains `replay` and `realtime paper`
    - `tests/unit/test_strategy_cli.py` contains `test_readme_documents_phase4_runtime_scope`
  </acceptance_criteria>
  <done>Phase 4 public docs match the locked runtime and reproducibility scope.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/unit/test_strategy_cli.py -q` passes
- [ ] `pytest -q` passes
- [ ] `rg "yaml|json|replay|realtime_paper" src/polymarket_quant/services/strategy_cli.py` finds CLI mode/config support
- [ ] `rg "max_drawdown|fill_rate|slippage" src/polymarket_quant/services/experiment_metrics.py` finds required metrics
- [ ] `rg "Phase 4 Strategy Runtime|manifest.json|target_exposure" README.md` finds required docs
</verification>

<success_criteria>
- Researchers can run replay or realtime paper from one CLI/config surface
- Resolved config and metrics are persisted alongside each run
- README describes the real Phase 4 boundary clearly
</success_criteria>

<output>
After completion, create `.planning/phases/04-strategy-research-workbench/04-05-SUMMARY.md`
</output>
