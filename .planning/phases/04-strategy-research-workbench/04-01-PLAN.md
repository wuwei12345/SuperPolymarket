---
phase: 04-strategy-research-workbench
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/polymarket_quant/domain/strategy.py
  - src/polymarket_quant/strategy/__init__.py
  - src/polymarket_quant/strategy/base.py
  - tests/unit/test_strategy_models.py
autonomous: true
requirements: [STRAT-01, STRAT-03]
user_setup: []
must_haves:
  truths:
    - Strategy implementations are Python classes with one shared lifecycle across modes.
    - Strategies emit target-based `Signal`, not `OrderIntent`.
    - Run modes, events, context snapshots, and resolved config models are explicit typed contracts.
  artifacts:
    - `domain/strategy.py` defines runtime and signal schemas.
    - `strategy/base.py` defines the minimum strategy class contract.
    - `test_strategy_models.py` covers identifiers, required fields, and lifecycle surface.
  key_links:
    - Later replay and realtime runners depend on this contract.
    - Later CLI/config work depends on the resolved config model defined here.
---

<objective>
Create the Phase 4 strategy domain foundation: typed signal, event, config, run manifest, and strategy base-class contracts that all later runtime plans will use.

Purpose: Without a locked strategy/runtime contract, replay, realtime paper, artifacts, and CLI work will drift into incompatible interfaces.
Output: strategy domain models, base strategy class, and tests.
</objective>

<execution_context>
@/Users/wuwei/Documents/polymarketQuantification/.codex/get-shit-done/workflows/execute-plan.md
@/Users/wuwei/Documents/polymarketQuantification/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-strategy-research-workbench/04-CONTEXT.md
@.planning/phases/04-strategy-research-workbench/04-RESEARCH.md
@.planning/phases/04-strategy-research-workbench/04-PATTERNS.md
@.planning/phases/04-strategy-research-workbench/04-VALIDATION.md
</context>

<threat_model>
## Threat Model

| ID | Threat | Severity | Mitigation Required In This Plan |
|----|--------|----------|-----------------------------------|
| T-04-01 | Replay and realtime paper diverge because they use different strategy contracts | high | One shared base strategy lifecycle and typed runtime models must be defined here |
| T-04-02 | Strategies bypass the alpha/sizing/execution split by emitting orders directly | high | Signal schema must be first-class and order schemas must remain in Phase 3 execution layer |
| T-04-03 | Missing resolved config fields make runs unreproducible before runtime work even begins | medium | Config and manifest schemas must be explicit and tested for required fields |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Define strategy runtime domain models</name>
  <files>src/polymarket_quant/domain/strategy.py, tests/unit/test_strategy_models.py</files>
  <read_first>src/polymarket_quant/domain/market_data.py, src/polymarket_quant/domain/simulation.py, .planning/phases/04-strategy-research-workbench/04-CONTEXT.md</read_first>
  <action>Create `src/polymarket_quant/domain/strategy.py` using Pydantic v2 with `ConfigDict(extra="forbid")`. Define enums/models for `RunMode`, `StrategyEventType`, `StrategySignal`, `StrategyEvent`, `UniverseSnapshot`, `ResolvedRunConfig`, `StrategyContextSnapshot`, and `RunManifest`. `StrategySignal` must support `token_id`, `target_exposure`, optional `target_position`, `ts`, optional `reason_code`, and optional `confidence`. `ResolvedRunConfig` must include strategy params, universe selection, sizing config, execution config, risk config, data-source config, and replay/realtime timing settings. Add validations rejecting blank identifiers and invalid mode values.</action>
  <verify>pytest tests/unit/test_strategy_models.py -q</verify>
  <acceptance_criteria>
    - `src/polymarket_quant/domain/strategy.py` contains `class StrategySignal`
    - `src/polymarket_quant/domain/strategy.py` contains `class RunManifest`
    - `src/polymarket_quant/domain/strategy.py` contains `class ResolvedRunConfig`
    - `src/polymarket_quant/domain/strategy.py` contains `target_exposure`
    - `tests/unit/test_strategy_models.py` contains `test_signal_requires_token_and_timestamp`
    - `tests/unit/test_strategy_models.py` contains `test_resolved_run_config_persists_required_sections`
  </acceptance_criteria>
  <done>Strategy/runtime data contracts are explicit and stable for later plans.</done>
</task>

<task type="auto">
  <name>Task 2: Define the base strategy lifecycle contract</name>
  <files>src/polymarket_quant/strategy/__init__.py, src/polymarket_quant/strategy/base.py, tests/unit/test_strategy_models.py</files>
  <read_first>src/polymarket_quant/domain/strategy.py, .planning/phases/04-strategy-research-workbench/04-CONTEXT.md</read_first>
  <action>Create `BaseStrategy` in `src/polymarket_quant/strategy/base.py` with lifecycle methods `on_init(self, ctx)`, `on_event(self, event, ctx)`, `on_clock(self, ts, ctx)`, and `on_finish(self, ctx)`. Methods may default to no-op or empty signal lists, but the contract must be importable and testable. Export the base symbols from `src/polymarket_quant/strategy/__init__.py`. Ensure docstrings capture the locked responsibilities from CONTEXT.md.</action>
  <verify>pytest tests/unit/test_strategy_models.py -q</verify>
  <acceptance_criteria>
    - `src/polymarket_quant/strategy/base.py` contains `class BaseStrategy`
    - `src/polymarket_quant/strategy/base.py` contains `def on_init`
    - `src/polymarket_quant/strategy/base.py` contains `def on_event`
    - `src/polymarket_quant/strategy/base.py` contains `def on_clock`
    - `src/polymarket_quant/strategy/base.py` contains `def on_finish`
    - `tests/unit/test_strategy_models.py` contains `test_base_strategy_exposes_required_lifecycle_methods`
  </acceptance_criteria>
  <done>Later runtimes can rely on one shared strategy lifecycle contract.</done>
</task>

<task type="auto">
  <name>Task 3: Add Phase 4 foundational dependencies only if the chosen implementation requires them</name>
  <files>pyproject.toml, tests/unit/test_strategy_models.py</files>
  <read_first>pyproject.toml, .planning/phases/04-strategy-research-workbench/04-RESEARCH.md</read_first>
  <action>If the chosen model/serialization implementation needs Phase 4 dependencies now, add only the minimal ones to `pyproject.toml`. This plan may add `PyYAML` early if config model parsing needs it, but leave parquet-specific packages for the artifact plan unless there is a clear direct need in Plan 04-01. Keep dependency churn scoped and justified by tests.</action>
  <verify>pytest tests/unit/test_strategy_models.py -q</verify>
  <acceptance_criteria>
    - `pyproject.toml` remains minimal and Phase-4-relevant
    - tests still run with the chosen dependency set
  </acceptance_criteria>
  <done>Foundation is added without front-loading unnecessary runtime packages.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/unit/test_strategy_models.py -q` passes
- [ ] `pytest -q` passes
- [ ] `rg "class StrategySignal|class RunManifest|class ResolvedRunConfig" src/polymarket_quant/domain/strategy.py` finds required models
- [ ] `rg "class BaseStrategy|def on_init|def on_event|def on_clock|def on_finish" src/polymarket_quant/strategy/base.py` finds lifecycle methods
</verification>

<success_criteria>
- Strategy lifecycle is defined once and is mode-agnostic
- Target-based `Signal` is the first-class output contract
- Resolved config and run manifest schemas are explicit and reusable
</success_criteria>

<output>
After completion, create `.planning/phases/04-strategy-research-workbench/04-01-SUMMARY.md`
</output>
