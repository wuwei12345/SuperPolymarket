---
phase: 04-strategy-research-workbench
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - src/polymarket_quant/services/strategy_runtime.py
  - src/polymarket_quant/services/replay_runtime.py
  - tests/unit/test_replay_runtime.py
autonomous: true
requirements: [STRAT-01, STRAT-02]
user_setup: []
must_haves:
  truths:
    - Replay runtime is event-driven underneath and supports time-based fixed-step decisions.
    - Replay mode reuses the same strategy class and context contract as later realtime paper mode.
    - Replay decisions do not leak future data into current strategy steps.
  artifacts:
    - `strategy_runtime.py` contains shared runtime/context orchestration.
    - `replay_runtime.py` contains replay-specific event/clock loop behavior.
    - `test_replay_runtime.py` covers event routing, clock triggering, and warmup-safe behavior.
  key_links:
    - Realtime paper mode in Plan 04-04 should reuse the same shared runtime abstractions.
    - Artifact writer from Plan 04-02 should be callable from replay runs.
---

<objective>
Build the Phase 4 replay runtime: a shared runtime core plus a replay-specific orchestrator that feeds market events and time-based clock ticks through the strategy lifecycle.

Purpose: Replay backtest is the safest place to validate the Phase 4 contract before wiring realtime paper mode.
Output: shared runtime helpers, replay runner, and tests.
</objective>

<execution_context>
@/Users/wuwei/Documents/polymarketQuantification/.codex/get-shit-done/workflows/execute-plan.md
@/Users/wuwei/Documents/polymarketQuantification/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-strategy-research-workbench/04-CONTEXT.md
@.planning/phases/04-strategy-research-workbench/04-RESEARCH.md
@.planning/phases/04-strategy-research-workbench/04-PATTERNS.md
@.planning/phases/04-strategy-research-workbench/04-VALIDATION.md
@.planning/phases/04-strategy-research-workbench/04-01-PLAN.md
@.planning/phases/04-strategy-research-workbench/04-02-PLAN.md
@.planning/phases/02-historical-real-time-data-platform/02-CONTEXT.md
@.planning/phases/03-simulation-exchange-portfolio-ledger/03-05-SUMMARY.md
</context>

<threat_model>
## Threat Model

| ID | Threat | Severity | Mitigation Required In This Plan |
|----|--------|----------|-----------------------------------|
| T-04-07 | Replay and realtime later diverge because replay builds a one-off strategy loop | high | Shared runtime/context orchestration must live outside the replay-specific wrapper |
| T-04-08 | Fixed-step logic uses future rows or post-step state, overstating backtest quality | high | Tests must assert time-based stepping only sees state available up to the decision timestamp |
| T-04-09 | Gap-filled or source-degraded data becomes invisible inside replay context | medium | Context and replay records must preserve source/gap markers from Phase 2 |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Build shared runtime orchestration helpers</name>
  <files>src/polymarket_quant/services/strategy_runtime.py, tests/unit/test_replay_runtime.py</files>
  <read_first>src/polymarket_quant/domain/strategy.py, src/polymarket_quant/domain/market_data.py, src/polymarket_quant/services/paper_exchange.py</read_first>
  <action>Create a shared runtime module that can initialize a strategy, build/update context snapshots, route events, trigger clock callbacks, and finalize a run. Keep it free of replay-only assumptions so realtime paper can reuse it later. Include explicit support for recent fills, recent risk decisions, positions, cash, feature snapshots, and current run metadata in context construction.</action>
  <verify>pytest tests/unit/test_replay_runtime.py -q</verify>
  <acceptance_criteria>
    - `src/polymarket_quant/services/strategy_runtime.py` contains `on_init`
    - `src/polymarket_quant/services/strategy_runtime.py` contains `on_event`
    - `src/polymarket_quant/services/strategy_runtime.py` contains `on_clock`
    - `src/polymarket_quant/services/strategy_runtime.py` contains `on_finish`
    - `tests/unit/test_replay_runtime.py` contains `test_runtime_context_includes_market_features_positions_and_risk`
  </acceptance_criteria>
  <done>One shared runtime core exists for all strategy modes.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the replay event + clock runner</name>
  <files>src/polymarket_quant/services/replay_runtime.py, tests/unit/test_replay_runtime.py</files>
  <read_first>src/polymarket_quant/services/strategy_runtime.py, src/polymarket_quant/services/market_data_queries.py, .planning/phases/04-strategy-research-workbench/04-CONTEXT.md</read_first>
  <action>Create a replay runner that consumes normalized market events or event-like rows in timestamp order, feeds them through `on_event()`, and triggers `on_clock()` on configured time-step boundaries (`1s` / `5s` / `1m` style). Ensure the runner can record signals into the artifact writer and that tests assert no future-data leakage and correct clock cadence.</action>
  <verify>pytest tests/unit/test_replay_runtime.py -q</verify>
  <acceptance_criteria>
    - `src/polymarket_quant/services/replay_runtime.py` contains `on_clock`
    - `src/polymarket_quant/services/replay_runtime.py` contains `time_step` or equivalent fixed-step configuration
    - `tests/unit/test_replay_runtime.py` contains `test_replay_runner_triggers_clock_on_time_boundaries`
    - `tests/unit/test_replay_runtime.py` contains `test_replay_runner_does_not_leak_future_rows`
    - `tests/unit/test_replay_runtime.py` contains `test_gap_fill_marker_survives_into_runtime_context`
  </acceptance_criteria>
  <done>Replay mode is usable as the first concrete strategy runtime.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/unit/test_replay_runtime.py -q` passes
- [ ] `pytest -q` passes
- [ ] `rg "time_step|on_clock|on_event" src/polymarket_quant/services/replay_runtime.py src/polymarket_quant/services/strategy_runtime.py` finds required runtime hooks
- [ ] `rg "test_replay_runner_triggers_clock_on_time_boundaries|test_replay_runner_does_not_leak_future_rows" tests/unit/test_replay_runtime.py` finds required tests
</verification>

<success_criteria>
- Replay runner is event-driven under the hood
- Time-based fixed-step decisions work without future leakage
- Shared runtime abstractions are reusable by realtime paper mode
</success_criteria>

<output>
After completion, create `.planning/phases/04-strategy-research-workbench/04-03-SUMMARY.md`
</output>
