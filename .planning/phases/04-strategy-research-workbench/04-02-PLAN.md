---
phase: 04-strategy-research-workbench
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - pyproject.toml
  - src/polymarket_quant/services/run_artifacts.py
  - tests/unit/test_run_artifacts.py
autonomous: true
requirements: [OPS-02, STRAT-03]
user_setup: []
must_haves:
  truths:
    - Every run persists a manifest plus reproducible artifacts in one run directory.
    - Saved config is resolved, not just a pointer to a config file.
    - Artifact writing is deterministic enough for unit testing with temporary directories.
  artifacts:
    - `run_artifacts.py` writes `manifest.json`, parquet artifacts, and text logs.
    - `test_run_artifacts.py` covers manifest shape and artifact file creation.
  key_links:
    - Replay and realtime runners use this plan to persist explainable run outputs.
    - CLI and metrics plans depend on the artifact directory contract defined here.
---

<objective>
Implement the run manifest and artifact bundle writer for Phase 4: one directory per run with resolved config, data scope, execution evidence, metrics placeholders, and logs.

Purpose: Reproducibility and comparability are hard requirements for Phase 4, so artifact persistence must exist before runtime orchestration is considered complete.
Output: artifact writer service, dependency updates for parquet/config support, and tests.
</objective>

<execution_context>
@/Users/wuwei/Documents/polymarketQuantification/.codex/get-shit-done/workflows/execute-plan.md
@/Users/wuwei/Documents/polymarketQuantification/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-strategy-research-workbench/04-CONTEXT.md
@.planning/phases/04-strategy-research-workbench/04-RESEARCH.md
@.planning/phases/04-strategy-research-workbench/04-PATTERNS.md
@.planning/phases/04-strategy-research-workbench/04-VALIDATION.md
@.planning/phases/04-strategy-research-workbench/04-01-PLAN.md
</context>

<threat_model>
## Threat Model

| ID | Threat | Severity | Mitigation Required In This Plan |
|----|--------|----------|-----------------------------------|
| T-04-04 | Runs cannot be reproduced because config/data scope are not persisted as resolved values | high | Manifest must include resolved config and data snapshot sections, not only file paths |
| T-04-05 | Artifact bundles are incomplete or partially written, making post-run analysis misleading | high | Writer should create deterministic file names and tests should assert required outputs exist |
| T-04-06 | Parquet/YAML support is added ad hoc without explicit dependency control | medium | Add only justified dependencies with tests and keep the writer API narrow |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 4 artifact dependencies deliberately</name>
  <files>pyproject.toml, tests/unit/test_run_artifacts.py</files>
  <read_first>pyproject.toml, .planning/phases/04-strategy-research-workbench/04-RESEARCH.md</read_first>
  <action>Add the minimal dependencies required to support artifact bundles and config parsing. Expected additions are `PyYAML`, `pyarrow`, and `duckdb` if the chosen artifact/query implementation uses them. Keep additions Phase-4-scoped and confirm tests can import the new surface without live services.</action>
  <verify>pytest tests/unit/test_run_artifacts.py -q</verify>
  <acceptance_criteria>
    - `pyproject.toml` contains only the dependencies actually used by the writer/config surface
    - tests import the writer surface without optional-install ambiguity
  </acceptance_criteria>
  <done>Artifact infrastructure dependencies are explicit and testable.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the run manifest and artifact bundle writer</name>
  <files>src/polymarket_quant/services/run_artifacts.py, tests/unit/test_run_artifacts.py</files>
  <read_first>src/polymarket_quant/domain/strategy.py, .planning/phases/04-strategy-research-workbench/04-CONTEXT.md</read_first>
  <action>Create a service such as `RunArtifactBundleWriter` that creates one run directory per `run_id`, writes `manifest.json`, appends/writes parquet files for signals/intents/orders/fills/positions/cash/pnl/risk, and writes `strategy.log` / `framework.log`. The manifest must include run identity, resolved config, data scope, artifact file names, and timing metadata. Use temporary directories in tests to assert real files are produced.</action>
  <verify>pytest tests/unit/test_run_artifacts.py -q</verify>
  <acceptance_criteria>
    - `src/polymarket_quant/services/run_artifacts.py` contains `manifest.json`
    - `src/polymarket_quant/services/run_artifacts.py` contains `to_parquet` or equivalent explicit parquet write path
    - `src/polymarket_quant/services/run_artifacts.py` contains `strategy.log`
    - `src/polymarket_quant/services/run_artifacts.py` contains `framework.log`
    - `tests/unit/test_run_artifacts.py` contains `test_writer_creates_manifest_and_required_artifact_files`
    - `tests/unit/test_run_artifacts.py` contains `test_manifest_persists_resolved_config_and_data_scope`
  </acceptance_criteria>
  <done>Run outputs are persisted in a stable explainable bundle shape.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/unit/test_run_artifacts.py -q` passes
- [ ] `pytest -q` passes
- [ ] `rg "manifest.json|strategy.log|framework.log" src/polymarket_quant/services/run_artifacts.py` finds required artifact outputs
- [ ] `rg "test_writer_creates_manifest_and_required_artifact_files|test_manifest_persists_resolved_config_and_data_scope" tests/unit/test_run_artifacts.py` finds required tests
</verification>

<success_criteria>
- One run directory contains the required manifest, parquet artifacts, and logs
- Resolved config and data scope are preserved for reproducibility
- Artifact writing is testable without live APIs or databases
</success_criteria>

<output>
After completion, create `.planning/phases/04-strategy-research-workbench/04-02-SUMMARY.md`
</output>
