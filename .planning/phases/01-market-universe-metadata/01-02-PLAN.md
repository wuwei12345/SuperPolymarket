---
phase: 01-market-universe-metadata
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/polymarket_quant/adapters/__init__.py
  - src/polymarket_quant/adapters/polymarket.py
  - src/polymarket_quant/services/__init__.py
  - src/polymarket_quant/services/market_sync.py
  - tests/unit/test_market_sync.py
autonomous: true
requirements: [MKT-01, MKT-03]
user_setup: []
must_haves:
  truths:
    - Sync fetches Gamma and CLOB market listings and only writes active + accepting order markets
    - Sync timeline events expose started, source fetch, retry, success, and failure states
    - Normalization preserves field source provenance for UI display
  artifacts:
    - src/polymarket_quant/adapters/polymarket.py implements Gamma and CLOB clients
    - src/polymarket_quant/services/market_sync.py implements sync orchestration and normalization
    - tests/unit/test_market_sync.py covers filtering, ID mapping, source mapping, and retry events
  key_links:
    - MarketSyncService writes CanonicalMarket objects into MarketStore through upsert_markets
---

<objective>
Implement Polymarket market universe sync from official Gamma and CLOB public endpoints, including normalization, retry timeline events, and local store writes.

Purpose: Phase 1 requires real market-universe ingestion before the UI can show meaningful table rows.
Output: Testable API adapters and sync service that produce canonical market rows with source provenance.
</objective>

<execution_context>
@/Users/wuwei/Documents/polymarketQuantification/.codex/get-shit-done/workflows/execute-plan.md
@/Users/wuwei/Documents/polymarketQuantification/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-market-universe-metadata/01-SPEC.md
@.planning/phases/01-market-universe-metadata/01-CONTEXT.md
@.planning/phases/01-market-universe-metadata/01-RESEARCH.md
@.planning/phases/01-market-universe-metadata/01-VALIDATION.md
@.planning/phases/01-market-universe-metadata/01-01-SUMMARY.md
</context>

<threat_model>
## Threat Model

| ID | Threat | Severity | Mitigation Required In This Plan |
|----|--------|----------|-----------------------------------|
| T-01-01 | Gamma/CLOB payload mismatch creates rows with wrong token mapping | high | Normalizer must join by `condition_id` and validate two token IDs before writing |
| T-01-02 | External API transient failures silently produce partial market universe | high | Sync must emit failed/retrying events and must not report success unless normalization and store write complete |
| T-01-03 | API response size or cursor handling drops pages | medium | CLOB client must iterate `next_cursor` until exhausted or explicit max page guard is reached |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Implement public Polymarket API clients</name>
  <files>src/polymarket_quant/adapters/__init__.py, src/polymarket_quant/adapters/polymarket.py, tests/unit/test_market_sync.py</files>
  <read_first>src/polymarket_quant/domain/market.py, src/polymarket_quant/storage/market_store.py, .planning/phases/01-market-universe-metadata/01-RESEARCH.md, src/polymarket_quant/adapters/polymarket.py</read_first>
  <action>Create `GammaClient` with base URL `https://gamma-api.polymarket.com` and method `fetch_markets(limit: int = 500) -> list[dict[str, Any]]` that calls `/markets` with params `active=true`, `closed=false`, `limit=<limit>`. Create `ClobClient` with base URL `https://clob.polymarket.com` and method `fetch_simplified_markets(limit: int = 1000, max_pages: int = 20) -> list[dict[str, Any]]` that calls `/simplified-markets`, follows `next_cursor`, and returns concatenated `data`. Both clients must accept an optional `httpx.Client` for tests and must set a browser-like `User-Agent` header string `polymarket-quant-simulator/0.1`.</action>
  <verify>pytest tests/unit/test_market_sync.py -q</verify>
  <acceptance_criteria>
    - `src/polymarket_quant/adapters/polymarket.py` contains `class GammaClient`
    - `src/polymarket_quant/adapters/polymarket.py` contains `class ClobClient`
    - `src/polymarket_quant/adapters/polymarket.py` contains `https://gamma-api.polymarket.com`
    - `src/polymarket_quant/adapters/polymarket.py` contains `https://clob.polymarket.com`
    - `tests/unit/test_market_sync.py` contains `test_clob_client_follows_next_cursor`
  </acceptance_criteria>
  <done>Public API clients fetch mocked Gamma and CLOB market listings.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize Gamma and CLOB markets into canonical records</name>
  <files>src/polymarket_quant/services/__init__.py, src/polymarket_quant/services/market_sync.py, tests/unit/test_market_sync.py</files>
  <read_first>src/polymarket_quant/domain/market.py, src/polymarket_quant/adapters/polymarket.py, .planning/phases/01-market-universe-metadata/01-SPEC.md, .planning/phases/01-market-universe-metadata/01-CONTEXT.md</read_first>
  <action>Create `SyncEvent` dataclass with fields `timestamp`, `step`, `source`, `status`, and `message`. Create `MarketSyncResult` dataclass with fields `written_count`, `skipped_count`, `events`, and `errors`. Implement `normalize_markets(gamma_markets, clob_markets) -> tuple[list[CanonicalMarket], list[str]]`. The normalizer must index CLOB records by `condition_id`, parse Gamma `clobTokenIds` JSON when present, prefer CLOB token IDs from `tokens` with outcomes `Yes` and `No`, copy `question`, `category`, `liquidity`, `endDate`, `restricted`, `active`, and `acceptingOrders`, and build `MarketSourceMap` values: question/category/liquidity/end_date = `GAMMA`, condition_id = `NORMALIZED`, yes/no token = `CLOB`. Only return markets where `active` and `accepting_orders` are true and IDs are complete; append skip reasons for invalid rows.</action>
  <verify>pytest tests/unit/test_market_sync.py -q</verify>
  <acceptance_criteria>
    - `src/polymarket_quant/services/market_sync.py` contains `def normalize_markets`
    - `src/polymarket_quant/services/market_sync.py` contains `class SyncEvent`
    - `src/polymarket_quant/services/market_sync.py` contains `MarketSourceMap`
    - `tests/unit/test_market_sync.py` contains `test_normalize_keeps_only_active_accepting_markets`
    - `tests/unit/test_market_sync.py` contains `test_normalize_source_map_marks_gamma_and_clob_fields`
  </acceptance_criteria>
  <done>Normalization produces valid canonical records and skip reasons.</done>
</task>

<task type="auto">
  <name>Task 3: Implement retrying sync orchestration</name>
  <files>src/polymarket_quant/services/market_sync.py, tests/unit/test_market_sync.py</files>
  <read_first>src/polymarket_quant/adapters/polymarket.py, src/polymarket_quant/storage/market_store.py, src/polymarket_quant/services/market_sync.py, .planning/phases/01-market-universe-metadata/01-UI-SPEC.md</read_first>
  <action>Implement `MarketSyncService` with constructor accepting `gamma_client`, `clob_client`, `store`, and `max_attempts: int = 3`. Add method `sync_once() -> MarketSyncResult`. It must append timeline events for `Sync started`, `Gamma fetch started`, `Gamma fetch completed`, `CLOB fetch started`, `CLOB fetch completed`, `Normalization completed`, and `Sync succeeded`. On transient exceptions from client calls, it must retry up to `max_attempts`, append `retrying` events with source labels, and return `MarketSyncResult` with `errors` and final `Sync failed` event if attempts are exhausted. On success it must call `store.init_schema()` and `store.upsert_markets(markets)` and report the written count.</action>
  <verify>pytest tests/unit/test_market_sync.py -q</verify>
  <acceptance_criteria>
    - `src/polymarket_quant/services/market_sync.py` contains `class MarketSyncService`
    - `src/polymarket_quant/services/market_sync.py` contains `def sync_once`
    - `src/polymarket_quant/services/market_sync.py` contains `Retrying Gamma after request failure`
    - `tests/unit/test_market_sync.py` contains `test_sync_emits_retry_and_failure_events`
    - `tests/unit/test_market_sync.py` contains `test_sync_writes_only_valid_markets_to_store`
    - `pytest tests/unit/test_market_sync.py -q` exits 0
  </acceptance_criteria>
  <done>Sync service writes market universe rows and exposes timeline events.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/unit/test_market_sync.py -q` passes
- [ ] `pytest tests/unit/test_market_models.py tests/unit/test_market_store.py tests/unit/test_market_sync.py -q` passes
- [ ] `rg "class MarketSyncService|def normalize_markets|class GammaClient|class ClobClient" src/polymarket_quant` finds all required symbols
</verification>

<success_criteria>

- Gamma and CLOB clients are testable without live API calls
- Normalization keeps only active + accepting markets
- Canonical records include condition ID, Yes token ID, No token ID, and field source labels
- Sync timeline includes source fetch, retry, success, and failure events
- Unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-market-universe-metadata/01-02-SUMMARY.md`
</output>
