---
phase: 01-market-universe-metadata
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - src/polymarket_quant/ui/__init__.py
  - src/polymarket_quant/ui/contracts.py
  - src/polymarket_quant/ui/market_universe_app.py
  - tests/unit/test_market_universe_ui_contract.py
  - README.md
autonomous: true
requirements: [MKT-02, MKT-03]
user_setup: []
must_haves:
  truths:
    - Browser UI exposes a table-first Market Universe page with required columns
    - Filters apply immediately and never expand the result set beyond active + accepting markets
    - Sync timeline log is collapsible and shows source-labeled events
    - UI copy and visual constants follow 01-UI-SPEC.md
  artifacts:
    - src/polymarket_quant/ui/market_universe_app.py implements the Streamlit-style page
    - src/polymarket_quant/ui/contracts.py defines required columns, filters, copy, and layout constants
    - tests/unit/test_market_universe_ui_contract.py validates the UI contract
    - README.md documents how to run the Phase 1 UI
  key_links:
    - UI reads from MarketStore and triggers MarketSyncService without adding trading, PnL, details, or WebSocket behaviors
---

<objective>
Create the Phase 1 read-only Web page for viewing and validating the market universe table, filters, sync timeline, and source provenance.

Purpose: The user explicitly wants a browser page to see process and results; this plan turns the data foundation into an inspectable Phase 1 surface.
Output: Streamlit-style UI entrypoint, UI contract constants, tests, and run instructions.
</objective>

<execution_context>
@/Users/wuwei/Documents/polymarketQuantification/.codex/get-shit-done/workflows/execute-plan.md
@/Users/wuwei/Documents/polymarketQuantification/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-market-universe-metadata/01-SPEC.md
@.planning/phases/01-market-universe-metadata/01-CONTEXT.md
@.planning/phases/01-market-universe-metadata/01-UI-SPEC.md
@.planning/phases/01-market-universe-metadata/01-RESEARCH.md
@.planning/phases/01-market-universe-metadata/01-01-SUMMARY.md
@.planning/phases/01-market-universe-metadata/01-02-SUMMARY.md
</context>

<threat_model>
## Threat Model

| ID | Threat | Severity | Mitigation Required In This Plan |
|----|--------|----------|-----------------------------------|
| T-01-01 | UI displays invalid or non-accepting markets as valid default rows | high | UI must read through MarketStore filters and tests must assert default filters preserve active + accepting behavior |
| T-01-02 | Source provenance is hidden from users, defeating Phase 1 traceability | medium | Required `source` column and source labels must be present in contract constants and UI table data |
| T-01-03 | Long IDs break layout or become unrecoverably truncated | medium | UI must apply controlled table overflow and document full ID availability through selection or tooltip |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Define UI contract constants</name>
  <files>src/polymarket_quant/ui/__init__.py, src/polymarket_quant/ui/contracts.py, tests/unit/test_market_universe_ui_contract.py</files>
  <read_first>.planning/phases/01-market-universe-metadata/01-UI-SPEC.md, .planning/phases/01-market-universe-metadata/01-CONTEXT.md, src/polymarket_quant/ui/contracts.py, tests/unit/test_market_universe_ui_contract.py</read_first>
  <action>Create `src/polymarket_quant/ui/contracts.py` with constants: `PAGE_TITLE = "Market Universe"`, `PRIMARY_CTA = "Sync markets"`, `DEFAULT_COLUMNS = ["question", "category", "liquidity", "endDate", "conditionId", "yes token", "no token", "source"]`, `REQUIRED_FILTERS = ["category", "minimum liquidity", "end date range", "restricted status", "question search"]`, `TIMELINE_EVENT_TYPES = ["Sync started", "Gamma fetch started", "Gamma fetch completed", "CLOB fetch started", "CLOB fetch completed", "Retry scheduled", "Retry failed", "Normalization completed", "Sync succeeded", "Sync failed"]`, and `LAYOUT_REGIONS = {"filters": "left", "table": "right", "timeline": "bottom-collapsible"}`. Add tests asserting these exact constants exist and include no detail panel region.</action>
  <verify>pytest tests/unit/test_market_universe_ui_contract.py -q</verify>
  <acceptance_criteria>
    - `src/polymarket_quant/ui/contracts.py` contains `PAGE_TITLE = "Market Universe"`
    - `src/polymarket_quant/ui/contracts.py` contains `DEFAULT_COLUMNS`
    - `src/polymarket_quant/ui/contracts.py` contains `bottom-collapsible`
    - `tests/unit/test_market_universe_ui_contract.py` contains `test_default_columns_match_ui_spec`
    - `pytest tests/unit/test_market_universe_ui_contract.py -q` exits 0
  </acceptance_criteria>
  <done>UI contract constants encode the locked UI-SPEC decisions.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Streamlit-style Market Universe page</name>
  <files>src/polymarket_quant/ui/market_universe_app.py, tests/unit/test_market_universe_ui_contract.py</files>
  <read_first>src/polymarket_quant/ui/contracts.py, src/polymarket_quant/storage/market_store.py, src/polymarket_quant/services/market_sync.py, .planning/phases/01-market-universe-metadata/01-UI-SPEC.md</read_first>
  <action>Implement `market_universe_app.py` with a `main()` function and helper functions `build_market_dataframe(markets)`, `apply_ui_filters(df, filters)`, and `render_timeline(events, expanded: bool)`. Use Streamlit import `import streamlit as st`. The page title must use `PAGE_TITLE`. The sync button text must use `PRIMARY_CTA`. The filter area must include category, minimum liquidity, end date range, restricted status, and question search controls. The table must render only `DEFAULT_COLUMNS` by default. The timeline must render bottom-collapsible behavior using a checkbox or expander labeled `Sync timeline`. Do not add detail panel, trading buttons, PnL views, WebSocket controls, wallet/auth controls, or row drill-down.</action>
  <verify>pytest tests/unit/test_market_universe_ui_contract.py -q</verify>
  <acceptance_criteria>
    - `src/polymarket_quant/ui/market_universe_app.py` contains `def main(`
    - `src/polymarket_quant/ui/market_universe_app.py` contains `st.set_page_config`
    - `src/polymarket_quant/ui/market_universe_app.py` contains `Sync timeline`
    - `src/polymarket_quant/ui/market_universe_app.py` does not contain `Trade`
    - `src/polymarket_quant/ui/market_universe_app.py` does not contain `PnL`
    - `tests/unit/test_market_universe_ui_contract.py` contains `test_ui_module_does_not_define_out_of_scope_surfaces`
  </acceptance_criteria>
  <done>Market Universe page can render required filters, table, and timeline without out-of-scope surfaces.</done>
</task>

<task type="auto">
  <name>Task 3: Document and verify Phase 1 UI run path</name>
  <files>README.md, tests/unit/test_market_universe_ui_contract.py</files>
  <read_first>src/polymarket_quant/ui/market_universe_app.py, .planning/phases/01-market-universe-metadata/01-UI-SPEC.md, README.md</read_first>
  <action>Create or update `README.md` with section `## Phase 1 Market Universe UI`. Include exact command `streamlit run src/polymarket_quant/ui/market_universe_app.py`. Document that Phase 1 defaults to `active + accepting orders`, shows left filters, right table, bottom collapsible timeline, and visible Gamma/CLOB source labels. Add or update tests to assert README contains the run command and the phrase `active + accepting orders`.</action>
  <verify>pytest tests/unit/test_market_universe_ui_contract.py -q</verify>
  <acceptance_criteria>
    - `README.md` contains `## Phase 1 Market Universe UI`
    - `README.md` contains `streamlit run src/polymarket_quant/ui/market_universe_app.py`
    - `README.md` contains `active + accepting orders`
    - `pytest tests/unit/test_market_universe_ui_contract.py -q` exits 0
  </acceptance_criteria>
  <done>Developer can run and understand the Phase 1 UI entrypoint.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/unit/test_market_universe_ui_contract.py -q` passes
- [ ] `pytest -q` passes
- [ ] `rg "Market Universe|Sync markets|Sync timeline|active \\+ accepting orders" src README.md` finds required UI copy and docs
- [ ] `rg "Trade|PnL|wallet|WebSocket" src/polymarket_quant/ui` does not find out-of-scope UI controls
</verification>

<success_criteria>

- UI-SPEC decisions are represented in constants and UI code
- UI exposes required default columns and filters
- Timeline log is bottom-collapsible and source-aware
- README documents the exact run command
- Tests verify contract and out-of-scope exclusions
</success_criteria>

<output>
After completion, create `.planning/phases/01-market-universe-metadata/01-03-SUMMARY.md`
</output>
