---
phase: 01-market-universe-metadata
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/polymarket_quant/__init__.py
  - src/polymarket_quant/domain/__init__.py
  - src/polymarket_quant/domain/market.py
  - src/polymarket_quant/storage/__init__.py
  - src/polymarket_quant/storage/market_store.py
  - tests/conftest.py
  - tests/unit/test_market_models.py
  - tests/unit/test_market_store.py
autonomous: true
requirements: [MKT-01, MKT-03]
user_setup: []
must_haves:
  truths:
    - Canonical market records require condition_id, yes_token_id, and no_token_id
    - Market source labels preserve whether a field came from Gamma, CLOB, or normalization
    - Local store can persist and query Phase 1 market universe rows without calling external APIs
  artifacts:
    - pyproject.toml declares Phase 1 runtime and test dependencies
    - src/polymarket_quant/domain/market.py defines canonical market models
    - src/polymarket_quant/storage/market_store.py defines local market persistence
    - tests/unit/test_market_models.py and tests/unit/test_market_store.py cover model/store behavior
  key_links:
    - MarketStore persists CanonicalMarket objects from domain.market without dropping ID or source provenance fields
---

<objective>
Create the Phase 1 Python package foundation: dependency configuration, canonical market data models, and a local SQLite-backed market store.

Purpose: Later sync and UI plans need stable contracts for Polymarket market metadata before they touch live APIs or render tables.
Output: A testable Python package with canonical market records, source provenance, and local persistence.
</objective>

<execution_context>
@/Users/wuwei/Documents/polymarketQuantification/.codex/get-shit-done/workflows/execute-plan.md
@/Users/wuwei/Documents/polymarketQuantification/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-market-universe-metadata/01-SPEC.md
@.planning/phases/01-market-universe-metadata/01-CONTEXT.md
@.planning/phases/01-market-universe-metadata/01-UI-SPEC.md
@.planning/phases/01-market-universe-metadata/01-RESEARCH.md
@.planning/phases/01-market-universe-metadata/01-VALIDATION.md
</context>

<threat_model>
## Threat Model

| ID | Threat | Severity | Mitigation Required In This Plan |
|----|--------|----------|-----------------------------------|
| T-01-01 | Malformed external market payloads produce valid-looking local records with missing or swapped IDs | high | CanonicalMarket validation must reject empty `condition_id`, `yes_token_id`, and `no_token_id`; tests must prove invalid records fail |
| T-01-02 | Source provenance is lost during persistence, making Gamma/CLOB field origins unverifiable in the UI | medium | Store schema must include source labels and tests must round-trip them |
| T-01-03 | Long token IDs or condition IDs are truncated in storage instead of only in UI display | medium | Store IDs as full text; tests must assert full ID round-trip |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Python package and pytest config</name>
  <files>pyproject.toml, src/polymarket_quant/__init__.py, src/polymarket_quant/domain/__init__.py, src/polymarket_quant/storage/__init__.py, tests/conftest.py</files>
  <read_first>.planning/phases/01-market-universe-metadata/01-RESEARCH.md, .planning/phases/01-market-universe-metadata/01-VALIDATION.md, AGENTS.md</read_first>
  <action>Create `pyproject.toml` with project name `polymarket-quant-simulator`, Python requirement `>=3.12`, package discovery under `src`, and dependencies: `httpx>=0.27`, `pydantic>=2.7`, `tenacity>=8.2`, `streamlit>=1.35`, `pandas>=2.2`. Add optional dev dependencies: `pytest>=8.2`, `respx>=0.21`, `pytest-asyncio>=0.23`. Configure pytest with `testpaths = ["tests"]` and `pythonpath = ["src"]`. Create empty package init files for `polymarket_quant`, `domain`, and `storage`. Create `tests/conftest.py` with a `tmp_db_path` fixture returning a temporary SQLite path.</action>
  <verify>python -m pytest --collect-only -q</verify>
  <acceptance_criteria>
    - `pyproject.toml` contains `name = "polymarket-quant-simulator"`
    - `pyproject.toml` contains `pythonpath = ["src"]`
    - `pyproject.toml` contains `streamlit>=1.35`
    - `tests/conftest.py` contains `def tmp_db_path(`
  </acceptance_criteria>
  <done>Package skeleton and pytest configuration exist, and pytest collection runs.</done>
</task>

<task type="auto">
  <name>Task 2: Define canonical market models and source provenance</name>
  <files>src/polymarket_quant/domain/market.py, tests/unit/test_market_models.py</files>
  <read_first>.planning/phases/01-market-universe-metadata/01-SPEC.md, .planning/phases/01-market-universe-metadata/01-CONTEXT.md, .planning/phases/01-market-universe-metadata/01-RESEARCH.md, src/polymarket_quant/domain/market.py, tests/unit/test_market_models.py</read_first>
  <action>Implement `SourceLabel` enum with values `GAMMA`, `CLOB`, and `NORMALIZED`. Implement `MarketSourceMap` Pydantic model with fields `question`, `category`, `liquidity`, `end_date`, `condition_id`, `yes_token_id`, `no_token_id`. Implement `CanonicalMarket` Pydantic model with fields: `market_id: str | None`, `question: str`, `category: str | None`, `liquidity: float | None`, `end_date: datetime | None`, `condition_id: str`, `yes_token_id: str`, `no_token_id: str`, `active: bool`, `accepting_orders: bool`, `restricted: bool | None`, `source_map: MarketSourceMap`, `raw_gamma: dict[str, Any] | None`, `raw_clob: dict[str, Any] | None`. Add validators that reject blank `condition_id`, `yes_token_id`, and `no_token_id`. Add method `is_phase1_valid()` returning true only when `active` and `accepting_orders` are true and required IDs are non-empty. Write tests proving valid records instantiate, blank IDs raise validation errors, source labels are present, and `is_phase1_valid()` rejects inactive or non-accepting markets.</action>
  <verify>pytest tests/unit/test_market_models.py -q</verify>
  <acceptance_criteria>
    - `src/polymarket_quant/domain/market.py` contains `class SourceLabel`
    - `src/polymarket_quant/domain/market.py` contains `class CanonicalMarket`
    - `src/polymarket_quant/domain/market.py` contains `def is_phase1_valid`
    - `tests/unit/test_market_models.py` contains `test_blank_ids_are_rejected`
    - `pytest tests/unit/test_market_models.py -q` exits 0
  </acceptance_criteria>
  <done>Canonical model behavior is implemented and unit-tested.</done>
</task>

<task type="auto">
  <name>Task 3: Implement local market store</name>
  <files>src/polymarket_quant/storage/market_store.py, tests/unit/test_market_store.py</files>
  <read_first>src/polymarket_quant/domain/market.py, src/polymarket_quant/storage/market_store.py, tests/unit/test_market_store.py</read_first>
  <action>Implement `MarketStore` using Python `sqlite3`. Constructor accepts `db_path: Path | str`. Add `init_schema()` creating table `markets` with text primary key `condition_id`, text columns for `market_id`, `question`, `category`, `end_date`, `yes_token_id`, `no_token_id`, JSON text columns `source_map`, `raw_gamma`, `raw_clob`, numeric `liquidity`, integer booleans `active`, `accepting_orders`, `restricted`, and text `updated_at`. Add `upsert_markets(markets: Sequence[CanonicalMarket]) -> int` that validates `market.is_phase1_valid()` before writing and returns written row count. Add `list_markets(category: str | None = None, min_liquidity: float | None = None, restricted: bool | None = None) -> list[CanonicalMarket]` that always returns only `active = 1` and `accepting_orders = 1` rows. Tests must prove full ID/source round-trip, invalid markets are not inserted, and filters change result sets.</action>
  <verify>pytest tests/unit/test_market_store.py -q</verify>
  <acceptance_criteria>
    - `src/polymarket_quant/storage/market_store.py` contains `class MarketStore`
    - `src/polymarket_quant/storage/market_store.py` contains `def upsert_markets`
    - `src/polymarket_quant/storage/market_store.py` contains `def list_markets`
    - `tests/unit/test_market_store.py` contains `test_list_markets_filters_active_accepting_only`
    - `pytest tests/unit/test_market_store.py -q` exits 0
  </acceptance_criteria>
  <done>Local persistence supports canonical market records and query filters.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/unit/test_market_models.py tests/unit/test_market_store.py -q` passes
- [ ] `python -m pytest --collect-only -q` passes
- [ ] `rg "class CanonicalMarket|class MarketStore|def is_phase1_valid" src/polymarket_quant` finds all three symbols
</verification>

<success_criteria>

- Python package skeleton exists under `src/polymarket_quant`
- Canonical market model rejects missing required IDs
- Store persists full IDs and source provenance
- Store queries only return active + accepting markets
- Unit tests for model and store pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-market-universe-metadata/01-01-SUMMARY.md`
</output>
