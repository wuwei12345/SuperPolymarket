---
phase: 02-historical-real-time-data-platform
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - src/polymarket_quant/adapters/__init__.py
  - src/polymarket_quant/adapters/polymarket_ws.py
  - src/polymarket_quant/services/__init__.py
  - src/polymarket_quant/services/realtime_collector.py
  - tests/unit/test_realtime_collector.py
  - README.md
autonomous: true
requirements: [DATA-02, DATA-03]
user_setup:
  - "For live smoke only: DATABASE_URL must be set and Phase 2 reference tokens must exist from backfill."
must_haves:
  truths:
    - Market WebSocket subscriptions use token IDs in `assets_ids`.
    - `custom_feature_enabled: true` is sent so `best_bid_ask` events are available.
    - Raw WebSocket events are persisted exactly before normalization.
    - Reconnect handling records a gap interval and repairs it with REST snapshot plus recent price history.
    - Hot/warm/cold pool defaults are configurable and do not require manual token selection.
  artifacts:
    - Async WebSocket adapter constructs official subscription payloads.
    - Realtime collector handles book, price_change, best_bid_ask, last_trade_price, and tick_size_change events.
    - Gap-fill service path calls the REST backfill/snapshot functionality with `gap_fill = true`.
    - Tests cover subscription payloads, event normalization, raw-first writes, reconnect, and gap repair.
  key_links:
    - Realtime collector writes through MarketDataStore and reuses MarketDataBackfillService repair helpers.
---

<objective>
Implement public market WebSocket collection for the selected top-N token universe, including raw event persistence, normalized latest-state updates, hot/warm/cold scheduling, and reconnect gap filling.

Purpose: Phase 2 must move beyond REST snapshots by continuously capturing replayable market events for later strategy and research-grade replay.
Output: WebSocket adapter, realtime collector service, gap-fill behavior, tests, and README run notes.
</objective>

<execution_context>
@/Users/wuwei/Documents/polymarketQuantification/.codex/get-shit-done/workflows/execute-plan.md
@/Users/wuwei/Documents/polymarketQuantification/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-historical-real-time-data-platform/02-CONTEXT.md
@.planning/phases/02-historical-real-time-data-platform/02-RESEARCH.md
@.planning/phases/02-historical-real-time-data-platform/02-VALIDATION.md
@.planning/phases/02-historical-real-time-data-platform/02-PATTERNS.md
@.planning/phases/02-historical-real-time-data-platform/02-01-SUMMARY.md
@.planning/phases/02-historical-real-time-data-platform/02-02-SUMMARY.md
</context>

<threat_model>
## Threat Model

| ID | Threat | Severity | Mitigation Required In This Plan |
|----|--------|----------|-----------------------------------|
| T-02-07 | Collector subscribes with condition IDs instead of token IDs and receives no useful market events | high | WebSocket adapter must send `assets_ids` from ReferenceToken.token_id and tests assert payload |
| T-02-08 | Normalization errors cause raw market events to be lost | high | Collector must call `insert_raw_ws_event` before dispatching event-specific normalizers |
| T-02-09 | Reconnects create unmarked data holes | high | Collector must record `GapFillInterval` and write repaired REST records with `gap_fill = true` |
| T-02-10 | Unbounded reconnect loop hammers endpoints during outage | medium | Use bounded exponential retry and timeline failure status after max attempts |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Implement market WebSocket adapter</name>
  <files>src/polymarket_quant/adapters/__init__.py, src/polymarket_quant/adapters/polymarket_ws.py, tests/unit/test_realtime_collector.py</files>
  <read_first>.planning/phases/02-historical-real-time-data-platform/02-RESEARCH.md, src/polymarket_quant/adapters/polymarket.py</read_first>
  <action>Create `polymarket_ws.py` with constant `MARKET_WS_URL = "wss://ws-subscriptions-clob.polymarket.com/ws/market"`. Add `MarketWebSocketClient` accepting `url: str = MARKET_WS_URL` and optional `connector` for tests. Implement `build_subscription_payload(token_ids: list[str]) -> dict[str, object]` returning `{"assets_ids": token_ids, "type": "market", "custom_feature_enabled": True}`. Implement async generator `subscribe(token_ids: list[str])` that connects, sends the JSON payload, yields parsed JSON messages, and ignores empty heartbeat text. Export the client from `adapters/__init__.py`.</action>
  <verify>pytest tests/unit/test_realtime_collector.py -q</verify>
  <acceptance_criteria>
    - `src/polymarket_quant/adapters/polymarket_ws.py` contains `MARKET_WS_URL = "wss://ws-subscriptions-clob.polymarket.com/ws/market"`
    - `src/polymarket_quant/adapters/polymarket_ws.py` contains `custom_feature_enabled`
    - `src/polymarket_quant/adapters/polymarket_ws.py` contains `assets_ids`
    - `src/polymarket_quant/adapters/polymarket_ws.py` contains `async def subscribe`
    - `tests/unit/test_realtime_collector.py` contains `test_market_ws_subscription_payload_uses_assets_ids_and_custom_feature`
  </acceptance_criteria>
  <done>WebSocket adapter sends the official market-channel subscription payload.</done>
</task>

<task type="auto">
  <name>Task 2: Implement realtime event normalization with raw-first persistence</name>
  <files>src/polymarket_quant/services/realtime_collector.py, tests/unit/test_realtime_collector.py</files>
  <read_first>src/polymarket_quant/adapters/polymarket_ws.py, src/polymarket_quant/storage/market_data_store.py, src/polymarket_quant/domain/market_data.py, .planning/phases/02-historical-real-time-data-platform/02-CONTEXT.md</read_first>
  <action>Create `RealtimeCollectorEvent`, `RealtimeCollectorResult`, and `MarketRealtimeCollector`. Constructor accepts `ws_client`, `store`, `tokens`, `max_messages: int | None = None`, and `connection_id: str | None = None`. Implement `collect_once()` that iterates WebSocket messages, creates `RawPayloadEnvelope(source="CLOB_WS", payload=message)`, calls `store.insert_raw_ws_event(envelope)` before normalization, then dispatches event types: `book` creates `BookSnapshot` and levels; `price_change` updates BBO from nested `price_changes[*].best_bid/best_ask`; `best_bid_ask` updates `BestBidAsk`; `last_trade_price` updates `LastTrade`; `tick_size_change` records a raw event and emits a timeline event but does not require normalized table beyond raw payload. Unknown event types remain raw and produce warning events.</action>
  <verify>pytest tests/unit/test_realtime_collector.py -q</verify>
  <acceptance_criteria>
    - `src/polymarket_quant/services/realtime_collector.py` contains `class MarketRealtimeCollector`
    - `src/polymarket_quant/services/realtime_collector.py` contains `insert_raw_ws_event`
    - `src/polymarket_quant/services/realtime_collector.py` contains `best_bid_ask`
    - `src/polymarket_quant/services/realtime_collector.py` contains `last_trade_price`
    - `src/polymarket_quant/services/realtime_collector.py` contains `tick_size_change`
    - `tests/unit/test_realtime_collector.py` contains `test_collector_persists_raw_ws_event_before_normalizing`
    - `tests/unit/test_realtime_collector.py` contains `test_collector_normalizes_best_bid_ask_and_last_trade`
  </acceptance_criteria>
  <done>Realtime collector stores raw events and derives normalized latest-state records.</done>
</task>

<task type="auto">
  <name>Task 3: Implement hot/warm/cold subscription scheduler</name>
  <files>src/polymarket_quant/services/realtime_collector.py, tests/unit/test_realtime_collector.py</files>
  <read_first>src/polymarket_quant/services/universe_selector.py, src/polymarket_quant/services/realtime_collector.py, .planning/phases/02-historical-real-time-data-platform/02-CONTEXT.md</read_first>
  <action>Add `SubscriptionPoolConfig` with defaults `hot_size=50`, `warm_size=100`, `warm_rotation_seconds=600`, and `cold_sample_seconds=3600`. Add pure helper `assign_subscription_pools(tokens, config) -> SubscriptionPools` returning `hot`, `warm`, and `cold` token lists ordered by `universe_rank`. Add `next_subscription_batch(now)` behavior that always includes hot tokens, includes the current warm rotation slice, and includes cold tokens only when their sample interval has elapsed. Keep scheduler deterministic and unit-testable without real sleeping.</action>
  <verify>pytest tests/unit/test_realtime_collector.py -q</verify>
  <acceptance_criteria>
    - `src/polymarket_quant/services/realtime_collector.py` contains `class SubscriptionPoolConfig`
    - `src/polymarket_quant/services/realtime_collector.py` contains `hot_size=50`
    - `src/polymarket_quant/services/realtime_collector.py` contains `warm_rotation_seconds=600`
    - `src/polymarket_quant/services/realtime_collector.py` contains `cold_sample_seconds=3600`
    - `tests/unit/test_realtime_collector.py` contains `test_subscription_pool_assigns_hot_warm_cold_by_rank`
    - `tests/unit/test_realtime_collector.py` contains `test_subscription_scheduler_keeps_hot_tokens_resident`
  </acceptance_criteria>
  <done>Token subscription pools are automatic and configurable.</done>
</task>

<task type="auto">
  <name>Task 4: Implement reconnect gap fill</name>
  <files>src/polymarket_quant/services/realtime_collector.py, tests/unit/test_realtime_collector.py, README.md</files>
  <read_first>src/polymarket_quant/services/backfill.py, src/polymarket_quant/storage/market_data_store.py, src/polymarket_quant/domain/market_data.py, .planning/phases/02-historical-real-time-data-platform/02-RESEARCH.md</read_first>
  <action>Add `GapFillService` with constructor accepting `clob_client`, `store`, `recent_history_minutes: int = 60`, and `book_chunk_size: int = 100`. Implement `fill_gap(token_ids, gap_started_at, gap_ended_at, connection_id)` that records `GapFillInterval`, fetches `/books` snapshots and recent `/batch-prices-history`, inserts raw REST payloads, normalizes repaired records with `gap_fill=True`, and returns events with step `Gap fill completed`. Update `MarketRealtimeCollector` reconnect handler to call `GapFillService.fill_gap` after a socket exception and before resubscribing. Add README section `## Phase 2 Realtime Collector` with env vars `DATABASE_URL`, `POLYMARKET_TOP_N`, and command `python -m polymarket_quant.services.realtime_collector`.</action>
  <verify>pytest tests/unit/test_realtime_collector.py -q</verify>
  <acceptance_criteria>
    - `src/polymarket_quant/services/realtime_collector.py` contains `class GapFillService`
    - `src/polymarket_quant/services/realtime_collector.py` contains `recent_history_minutes: int = 60`
    - `src/polymarket_quant/services/realtime_collector.py` contains `record_gap_interval`
    - `src/polymarket_quant/services/realtime_collector.py` contains `gap_fill=True`
    - `README.md` contains `## Phase 2 Realtime Collector`
    - `README.md` contains `python -m polymarket_quant.services.realtime_collector`
    - `tests/unit/test_realtime_collector.py` contains `test_reconnect_records_gap_and_runs_gap_fill`
  </acceptance_criteria>
  <done>Reconnect repair writes marked gap-fill records and is documented for local smoke testing.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/unit/test_realtime_collector.py -q` passes
- [ ] `pytest -q` passes
- [ ] `rg "assets_ids|custom_feature_enabled|best_bid_ask|last_trade_price|tick_size_change" src/polymarket_quant` finds required WS behavior
- [ ] `rg "class GapFillService|record_gap_interval|gap_fill=True" src/polymarket_quant/services/realtime_collector.py` finds reconnect repair behavior
</verification>

<success_criteria>
- Market WS subscription uses token IDs and enables best bid/ask custom feature
- Raw WS events are persisted before normalization
- Event normalizers support book, price_change, best_bid_ask, last_trade_price, and tick_size_change
- Hot/warm/cold token pools are configurable and deterministic
- Reconnects create marked gap-fill intervals and repair records through REST snapshot/history
</success_criteria>

<output>
After completion, create `.planning/phases/02-historical-real-time-data-platform/02-03-SUMMARY.md`
</output>
