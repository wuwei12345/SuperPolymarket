---
phase: 02-historical-real-time-data-platform
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/polymarket_quant/domain/__init__.py
  - src/polymarket_quant/domain/market_data.py
  - src/polymarket_quant/storage/__init__.py
  - src/polymarket_quant/storage/postgres_schema.sql
  - src/polymarket_quant/storage/market_data_store.py
  - tests/unit/test_market_data_store.py
  - README.md
autonomous: true
requirements: [DATA-01, DATA-03]
user_setup:
  - "For live smoke only: set DATABASE_URL=postgresql://localhost/polymarket_quant or another disposable PostgreSQL DSN."
must_haves:
  truths:
    - PostgreSQL is the Phase 2 market-data store; Phase 1 SQLite MarketStore remains intact.
    - Storage is layered as reference, raw, normalized, and views.
    - Raw REST payloads and raw WebSocket events can be inserted before normalized records.
    - Normalized rows carry token_id, condition_id, source labels, source timestamps, received timestamps, and gap_fill markers.
  artifacts:
    - src/polymarket_quant/domain/market_data.py defines Phase 2 DTOs.
    - src/polymarket_quant/storage/postgres_schema.sql defines schemas reference/raw/normalized/views.
    - src/polymarket_quant/storage/market_data_store.py implements schema init and insert/upsert/query methods.
    - tests/unit/test_market_data_store.py verifies schema and repository contracts without requiring live PostgreSQL.
  key_links:
    - Later backfill and realtime plans write through MarketDataStore instead of writing SQL inline.
---

<objective>
Create the PostgreSQL data foundation for Phase 2: typed market-data DTOs, explicit layered schema, and a testable repository for raw and normalized records.

Purpose: Backfill, WebSocket collection, replay, and the inspection page all need a durable store with source/gap traceability before ingestion logic is added.
Output: PostgreSQL schema SQL, `MarketDataStore`, domain models, tests, and README setup notes.
</objective>

<execution_context>
@/Users/wuwei/Documents/polymarketQuantification/.codex/get-shit-done/workflows/execute-plan.md
@/Users/wuwei/Documents/polymarketQuantification/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-historical-real-time-data-platform/02-CONTEXT.md
@.planning/phases/02-historical-real-time-data-platform/02-RESEARCH.md
@.planning/phases/02-historical-real-time-data-platform/02-VALIDATION.md
@.planning/phases/02-historical-real-time-data-platform/02-PATTERNS.md
@.planning/phases/01-market-universe-metadata/01-VERIFICATION.md
</context>

<threat_model>
## Threat Model

| ID | Threat | Severity | Mitigation Required In This Plan |
|----|--------|----------|-----------------------------------|
| T-02-01 | Normalized-only storage loses auditability and prevents replay debugging | high | SQL must include raw REST and raw WS tables with JSONB payloads and repository insert methods |
| T-02-02 | Gap-filled data is indistinguishable from live data | high | All normalized time-series tables must include `gap_fill BOOLEAN NOT NULL DEFAULT false` and source fields |
| T-02-03 | PostgreSQL rollout breaks Phase 1 verified SQLite market universe UI | medium | Do not alter `MarketStore` behavior; add `MarketDataStore` separately |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 2 market-data domain models and dependencies</name>
  <files>pyproject.toml, src/polymarket_quant/domain/__init__.py, src/polymarket_quant/domain/market_data.py, tests/unit/test_market_data_store.py</files>
  <read_first>pyproject.toml, src/polymarket_quant/domain/market.py, .planning/phases/02-historical-real-time-data-platform/02-RESEARCH.md</read_first>
  <action>Add dependencies `psycopg[binary]>=3.2` and `websockets>=12.0` to `pyproject.toml`. Create `market_data.py` with Pydantic models or frozen dataclasses: `ReferenceToken`, `RawPayloadEnvelope`, `PriceHistoryPoint`, `BookLevel`, `BookSnapshot`, `BestBidAsk`, `LastTrade`, and `GapFillInterval`. Required fields: `token_id: str`, `condition_id: str | None`, `source: str`, `received_at: datetime`, `source_ts: datetime | None`, and `gap_fill: bool = False` on normalized time-series models. Add validators rejecting blank `token_id`. Export the module symbols from `domain/__init__.py`.</action>
  <verify>pytest tests/unit/test_market_data_store.py -q</verify>
  <acceptance_criteria>
    - `pyproject.toml` contains `psycopg[binary]>=3.2`
    - `pyproject.toml` contains `websockets>=12.0`
    - `src/polymarket_quant/domain/market_data.py` contains `class ReferenceToken`
    - `src/polymarket_quant/domain/market_data.py` contains `class RawPayloadEnvelope`
    - `src/polymarket_quant/domain/market_data.py` contains `class BestBidAsk`
    - `src/polymarket_quant/domain/market_data.py` contains `gap_fill: bool = False`
    - `tests/unit/test_market_data_store.py` contains `test_reference_token_rejects_blank_token_id`
  </acceptance_criteria>
  <done>Phase 2 has typed market-data records with source and gap fields.</done>
</task>

<task type="auto">
  <name>Task 2: Define layered PostgreSQL schema</name>
  <files>src/polymarket_quant/storage/postgres_schema.sql, tests/unit/test_market_data_store.py</files>
  <read_first>.planning/phases/02-historical-real-time-data-platform/02-CONTEXT.md, .planning/phases/02-historical-real-time-data-platform/02-RESEARCH.md, src/polymarket_quant/storage/market_store.py</read_first>
  <action>Create `postgres_schema.sql` with `CREATE SCHEMA IF NOT EXISTS reference;`, `CREATE SCHEMA IF NOT EXISTS raw;`, `CREATE SCHEMA IF NOT EXISTS normalized;`, and `CREATE SCHEMA IF NOT EXISTS views;`. Add tables `reference.tokens`, `reference.universe_runs`, `raw.rest_payloads`, `raw.websocket_events`, `raw.gap_fill_intervals`, `normalized.price_history`, `normalized.book_snapshots`, `normalized.book_levels`, `normalized.best_bid_ask`, and `normalized.last_trades`. Use `JSONB NOT NULL` for raw payload columns. Add indexes on `token_id`, `condition_id`, `source_ts`, `received_at`, and `gap_fill` where relevant. Add views `views.latest_market_state` and `views.price_series_recent`.</action>
  <verify>pytest tests/unit/test_market_data_store.py -q</verify>
  <acceptance_criteria>
    - `src/polymarket_quant/storage/postgres_schema.sql` contains `CREATE SCHEMA IF NOT EXISTS reference`
    - `src/polymarket_quant/storage/postgres_schema.sql` contains `CREATE SCHEMA IF NOT EXISTS raw`
    - `src/polymarket_quant/storage/postgres_schema.sql` contains `CREATE SCHEMA IF NOT EXISTS normalized`
    - `src/polymarket_quant/storage/postgres_schema.sql` contains `CREATE SCHEMA IF NOT EXISTS views`
    - `src/polymarket_quant/storage/postgres_schema.sql` contains `raw.websocket_events`
    - `src/polymarket_quant/storage/postgres_schema.sql` contains `views.latest_market_state`
    - `tests/unit/test_market_data_store.py` contains `test_postgres_schema_defines_required_layers`
  </acceptance_criteria>
  <done>Schema file encodes the required layered store and latest-state views.</done>
</task>

<task type="auto">
  <name>Task 3: Implement PostgreSQL market data repository</name>
  <files>src/polymarket_quant/storage/__init__.py, src/polymarket_quant/storage/market_data_store.py, tests/unit/test_market_data_store.py</files>
  <read_first>src/polymarket_quant/storage/market_store.py, src/polymarket_quant/storage/postgres_schema.sql, src/polymarket_quant/domain/market_data.py</read_first>
  <action>Create `MarketDataStore` with constructor `__init__(self, dsn: str | None = None, connection: Any | None = None)`. Resolve DSN from explicit `dsn` or `DATABASE_URL`. Implement `init_schema()`, `upsert_reference_tokens(tokens)`, `insert_raw_rest_payload(envelope)`, `insert_raw_ws_event(envelope)`, `upsert_price_history_points(points)`, `insert_book_snapshot(snapshot)`, `upsert_best_bid_ask(rows)`, `upsert_last_trades(rows)`, `record_gap_interval(interval)`, `fetch_latest_state(limit: int = 100)`, and `fetch_price_series(token_id: str, limit: int = 500)`. Load SQL from `postgres_schema.sql` using `Path(__file__).with_name("postgres_schema.sql")`. Use `psycopg.rows.dict_row` for real connections. Keep methods testable by accepting a fake connection with `execute` and `executemany`.</action>
  <verify>pytest tests/unit/test_market_data_store.py -q</verify>
  <acceptance_criteria>
    - `src/polymarket_quant/storage/market_data_store.py` contains `class MarketDataStore`
    - `src/polymarket_quant/storage/market_data_store.py` contains `def init_schema`
    - `src/polymarket_quant/storage/market_data_store.py` contains `def insert_raw_ws_event`
    - `src/polymarket_quant/storage/market_data_store.py` contains `def upsert_best_bid_ask`
    - `src/polymarket_quant/storage/market_data_store.py` contains `DATABASE_URL`
    - `tests/unit/test_market_data_store.py` contains `test_store_inserts_raw_payload_before_normalized_records_contract`
    - `pytest tests/unit/test_market_data_store.py -q` exits 0
  </acceptance_criteria>
  <done>Repository exposes the persistence contract used by later ingestion services.</done>
</task>

<task type="auto">
  <name>Task 4: Document local PostgreSQL setup and keep Phase 1 store stable</name>
  <files>README.md, tests/unit/test_market_data_store.py</files>
  <read_first>README.md, src/polymarket_quant/storage/market_store.py, src/polymarket_quant/storage/market_data_store.py</read_first>
  <action>Add README section `## Phase 2 Market Data Store`. Include env var `DATABASE_URL`, example `postgresql://localhost/polymarket_quant`, and statement `Phase 1 SQLite MarketStore remains the market universe source; Phase 2 imports reference tokens into PostgreSQL.` Add tests asserting `src/polymarket_quant/storage/market_store.py` still contains `sqlite3` and `MarketDataStore` is a separate class.</action>
  <verify>pytest tests/unit/test_market_data_store.py -q</verify>
  <acceptance_criteria>
    - `README.md` contains `## Phase 2 Market Data Store`
    - `README.md` contains `DATABASE_URL`
    - `README.md` contains `Phase 1 SQLite MarketStore remains`
    - `src/polymarket_quant/storage/market_store.py` contains `sqlite3`
    - `tests/unit/test_market_data_store.py` contains `test_phase1_market_store_remains_sqlite_backed`
  </acceptance_criteria>
  <done>Developer setup path is documented and Phase 1 persistence remains untouched.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/unit/test_market_data_store.py -q` passes
- [ ] `pytest -q` passes
- [ ] `rg "reference.tokens|raw.websocket_events|normalized.best_bid_ask|views.latest_market_state" src/polymarket_quant/storage/postgres_schema.sql` finds all required SQL objects
- [ ] `rg "class MarketDataStore|def insert_raw_ws_event|def record_gap_interval" src/polymarket_quant/storage/market_data_store.py` finds required repository APIs
</verification>

<success_criteria>
- PostgreSQL schemas reference/raw/normalized/views are declared in SQL
- Raw payload tables exist for REST and WebSocket data
- Normalized price/book/BBO/last-trade tables include source and gap markers
- Store class can be unit-tested without live PostgreSQL
- README documents `DATABASE_URL` and the Phase 1 SQLite boundary
</success_criteria>

<output>
After completion, create `.planning/phases/02-historical-real-time-data-platform/02-01-SUMMARY.md`
</output>
