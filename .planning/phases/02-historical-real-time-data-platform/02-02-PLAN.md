---
phase: 02-historical-real-time-data-platform
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/polymarket_quant/adapters/polymarket.py
  - src/polymarket_quant/services/__init__.py
  - src/polymarket_quant/services/universe_selector.py
  - src/polymarket_quant/services/backfill.py
  - tests/unit/test_backfill.py
  - README.md
autonomous: true
requirements: [DATA-01, DATA-04]
user_setup:
  - "For live smoke only: Phase 1 SQLite universe must contain synced markets and DATABASE_URL must point at a PostgreSQL database."
must_haves:
  truths:
    - Top-N tokens are selected automatically from Phase 1 active + accepting markets.
    - Backfill writes raw REST payloads before normalized price history or book-derived rows.
    - Each selected token gets price history backfill followed by a current book snapshot.
    - Derived spread and midpoint are computed from best bid/ask and marked as normalized fields.
    - Data API trades/activity is represented as an optional raw ingestion extension point, not a Phase 2 hard prerequisite.
  artifacts:
    - ClobClient supports batch price history, order books, prices, and last trade price requests.
    - Universe selector returns ranked ReferenceToken rows for Yes and No tokens.
    - Backfill service orchestrates price history then current snapshot per selected token.
    - Tests cover chunking, raw-first writes, derived BBO/spread/midpoint, and optional Data API raw hooks.
  key_links:
    - Backfill service writes through MarketDataStore from Plan 02-01.
---

<objective>
Implement automatic token universe selection and REST batch backfill for price history, current book snapshots, best bid/ask, spread, midpoint, and last trade fields.

Purpose: Phase 2 needs a historical/current data baseline before realtime collection and gap repair can be meaningful.
Output: Extended CLOB REST client, ranked universe selector, backfill service, tests, and run docs.
</objective>

<execution_context>
@/Users/wuwei/Documents/polymarketQuantification/.codex/get-shit-done/workflows/execute-plan.md
@/Users/wuwei/Documents/polymarketQuantification/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-historical-real-time-data-platform/02-CONTEXT.md
@.planning/phases/02-historical-real-time-data-platform/02-RESEARCH.md
@.planning/phases/02-historical-real-time-data-platform/02-VALIDATION.md
@.planning/phases/02-historical-real-time-data-platform/02-PATTERNS.md
@.planning/phases/02-historical-real-time-data-platform/02-01-SUMMARY.md
</context>

<threat_model>
## Threat Model

| ID | Threat | Severity | Mitigation Required In This Plan |
|----|--------|----------|-----------------------------------|
| T-02-04 | Backfill calls one token at a time and hits avoidable rate-limit pressure | medium | Use `/batch-prices-history` chunks of max 20 and `/books` batch snapshots |
| T-02-05 | Normalized rows exist without raw payload evidence | high | Backfill service must call `insert_raw_rest_payload` before normalized writes |
| T-02-06 | Spread/midpoint are mistaken for source fields | medium | Derived rows must use source label `Normalized` and preserve raw CLOB payload separately |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Extend public CLOB/Data REST adapters</name>
  <files>src/polymarket_quant/adapters/polymarket.py, tests/unit/test_backfill.py</files>
  <read_first>src/polymarket_quant/adapters/polymarket.py, .planning/phases/02-historical-real-time-data-platform/02-RESEARCH.md</read_first>
  <action>Extend `ClobClient` with methods `fetch_prices_history_batch(markets: list[str], start_ts: int | None = None, end_ts: int | None = None, interval: str = "1d", fidelity: int = 1) -> dict[str, Any]`, `fetch_order_books(token_ids: list[str]) -> list[dict[str, Any]]`, `fetch_market_prices(params: list[dict[str, str]]) -> dict[str, dict[str, float]]`, and `fetch_last_trade_prices(token_ids: list[str]) -> list[dict[str, Any]]`. Use endpoints `/batch-prices-history`, `/books`, `/prices`, and `/last-trades-prices`. Add `DATA_BASE_URL = "https://data-api.polymarket.com"` and `DataApiClient` with raw optional methods `fetch_trades(params: dict[str, object])` and `fetch_activity(params: dict[str, object])`, enforcing default `limit=500` and preserving raw JSON. All clients must accept injectable `httpx.Client` and set `User-Agent`.</action>
  <verify>pytest tests/unit/test_backfill.py -q</verify>
  <acceptance_criteria>
    - `src/polymarket_quant/adapters/polymarket.py` contains `def fetch_prices_history_batch`
    - `src/polymarket_quant/adapters/polymarket.py` contains `/batch-prices-history`
    - `src/polymarket_quant/adapters/polymarket.py` contains `def fetch_order_books`
    - `src/polymarket_quant/adapters/polymarket.py` contains `/last-trades-prices`
    - `src/polymarket_quant/adapters/polymarket.py` contains `class DataApiClient`
    - `tests/unit/test_backfill.py` contains `test_clob_client_posts_batch_prices_history`
    - `tests/unit/test_backfill.py` contains `test_data_api_client_fetches_trades_as_raw_optional_payload`
  </acceptance_criteria>
  <done>REST adapters expose official Phase 2 public data endpoints with mockable tests.</done>
</task>

<task type="auto">
  <name>Task 2: Implement automatic ranked token universe selection</name>
  <files>src/polymarket_quant/services/__init__.py, src/polymarket_quant/services/universe_selector.py, tests/unit/test_backfill.py</files>
  <read_first>src/polymarket_quant/storage/market_store.py, src/polymarket_quant/domain/market.py, src/polymarket_quant/domain/market_data.py, .planning/phases/02-historical-real-time-data-platform/02-CONTEXT.md</read_first>
  <action>Create `UniverseSelector` with constructor `__init__(self, market_store: MarketStore, top_n: int = 50)`. Implement `select_top_tokens() -> list[ReferenceToken]`. It must call `market_store.list_markets()`, rank markets by `liquidity` descending, then `end_date` ascending with nulls last, then `question` ascending, and emit two `ReferenceToken` rows per market: outcome `Yes` with `yes_token_id`, outcome `No` with `no_token_id`. Stop after `top_n` token rows, not markets. Include `universe_rank` and `selection_reason = "liquidity_desc_end_date_asc"`. Export `UniverseSelector` from `services/__init__.py`.</action>
  <verify>pytest tests/unit/test_backfill.py -q</verify>
  <acceptance_criteria>
    - `src/polymarket_quant/services/universe_selector.py` contains `class UniverseSelector`
    - `src/polymarket_quant/services/universe_selector.py` contains `top_n: int = 50`
    - `src/polymarket_quant/services/universe_selector.py` contains `selection_reason = "liquidity_desc_end_date_asc"`
    - `tests/unit/test_backfill.py` contains `test_universe_selector_ranks_active_accepting_tokens_by_liquidity`
    - `tests/unit/test_backfill.py` contains `test_universe_selector_limits_by_token_count_not_market_count`
  </acceptance_criteria>
  <done>Top-N token universe is automatic and deterministic.</done>
</task>

<task type="auto">
  <name>Task 3: Implement REST backfill orchestration</name>
  <files>src/polymarket_quant/services/backfill.py, tests/unit/test_backfill.py</files>
  <read_first>src/polymarket_quant/adapters/polymarket.py, src/polymarket_quant/storage/market_data_store.py, src/polymarket_quant/services/universe_selector.py, src/polymarket_quant/services/market_sync.py</read_first>
  <action>Create dataclasses `BackfillEvent` and `BackfillResult` mirroring `SyncEvent` style with fields `timestamp`, `step`, `source`, `status`, `message`, `token_id: str | None = None`, and `gap_fill: bool = False`. Create `MarketDataBackfillService` with constructor accepting `selector`, `clob_client`, `store`, `price_history_chunk_size: int = 20`, `book_chunk_size: int = 100`, `interval: str = "1d"`, `fidelity: int = 1`. Implement `backfill_top_tokens() -> BackfillResult`: initialize schema, select tokens, upsert reference tokens, fetch `/batch-prices-history` in chunks of 20, insert raw REST payloads, normalize `history` points into `PriceHistoryPoint`, then fetch `/books` snapshots, insert raw payloads, normalize book snapshots/levels plus `BestBidAsk` rows and `LastTrade` rows when `last_trade_price` is present. Compute `spread = best_ask - best_bid` and `midpoint = (best_bid + best_ask) / 2` using `Decimal` to avoid float drift.</action>
  <verify>pytest tests/unit/test_backfill.py -q</verify>
  <acceptance_criteria>
    - `src/polymarket_quant/services/backfill.py` contains `class MarketDataBackfillService`
    - `src/polymarket_quant/services/backfill.py` contains `price_history_chunk_size: int = 20`
    - `src/polymarket_quant/services/backfill.py` contains `Decimal`
    - `src/polymarket_quant/services/backfill.py` contains `insert_raw_rest_payload`
    - `tests/unit/test_backfill.py` contains `test_backfill_writes_raw_price_history_before_normalized_points`
    - `tests/unit/test_backfill.py` contains `test_backfill_fetches_book_snapshot_after_price_history`
    - `tests/unit/test_backfill.py` contains `test_backfill_derives_spread_and_midpoint_from_book`
  </acceptance_criteria>
  <done>Backfill produces raw and normalized price/book/current-state rows for selected tokens.</done>
</task>

<task type="auto">
  <name>Task 4: Document Phase 2 REST backfill command path</name>
  <files>README.md, tests/unit/test_backfill.py</files>
  <read_first>README.md, src/polymarket_quant/services/backfill.py, src/polymarket_quant/services/universe_selector.py</read_first>
  <action>Add README section `## Phase 2 REST Backfill`. Include exact env vars `DATABASE_URL` and `POLYMARKET_TOP_N`, state default `POLYMARKET_TOP_N=50`, and include example Python command `python -m polymarket_quant.services.backfill`. Implement a minimal module entrypoint in `backfill.py` guarded by `if __name__ == "__main__":` that reads env vars, creates `MarketStore("data/markets.sqlite3")`, `MarketDataStore()`, `ClobClient()`, `UniverseSelector`, runs `backfill_top_tokens()`, and prints written counts. Tests may assert command strings and entrypoint existence without making live calls.</action>
  <verify>pytest tests/unit/test_backfill.py -q</verify>
  <acceptance_criteria>
    - `README.md` contains `## Phase 2 REST Backfill`
    - `README.md` contains `POLYMARKET_TOP_N=50`
    - `README.md` contains `python -m polymarket_quant.services.backfill`
    - `src/polymarket_quant/services/backfill.py` contains `if __name__ == "__main__":`
    - `tests/unit/test_backfill.py` contains `test_readme_documents_phase2_backfill_command`
  </acceptance_criteria>
  <done>Developer can run a documented local REST backfill smoke after setup.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/unit/test_backfill.py -q` passes
- [ ] `pytest -q` passes
- [ ] `rg "batch-prices-history|/books|/prices|last-trades-prices" src/polymarket_quant/adapters/polymarket.py` finds official endpoints
- [ ] `rg "class UniverseSelector|class MarketDataBackfillService|insert_raw_rest_payload" src/polymarket_quant/services` finds required services
</verification>

<success_criteria>
- Top-N token universe is selected automatically from active + accepting Phase 1 markets
- Price history backfill uses official batch endpoint chunks of at most 20 token IDs
- Current book snapshots are fetched after price history backfill
- Raw REST payloads are written before normalized rows
- Derived spread, midpoint, and last trade values are persisted for inspection/replay
- Optional Data API raw hooks exist without making trades/activity a hard Phase 2 dependency
</success_criteria>

<output>
After completion, create `.planning/phases/02-historical-real-time-data-platform/02-02-SUMMARY.md`
</output>
