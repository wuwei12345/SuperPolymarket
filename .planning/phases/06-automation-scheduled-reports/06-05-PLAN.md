---
phase: 06-automation-scheduled-reports
plan: 05
type: execute
wave: 5
depends_on: [06-01, 06-02, 06-03, 06-04]
files_modified:
  - src/polymarket_quant/services/automation_cli.py
  - src/polymarket_quant/services/__init__.py
  - README.md
  - config/automation.daily.yaml
  - tests/unit/test_automation_cli.py
autonomous: true
requirements: [AUTO-01, AUTO-02, AUTO-03]
user_setup: []
must_haves:
  truths:
    - Phase 6's operator-facing entrypoint is a one-shot CLI, not a daemon scheduler.
    - README must include cron examples because scheduling stays with the host environment.
    - The default automated mode is `realtime_paper` and must not imply live trading.
  artifacts:
    - `automation_cli.py` provides the standard one-shot automation command.
    - `config/automation.daily.yaml` provides a concrete sample daily config.
    - README documents manual run and cron usage.
  key_links:
    - This plan turns the earlier Phase 6 internals into an operator-usable workflow.
---

<objective>
Ship the operator-facing Phase 6 workflow: a standard automation CLI, a sample daily YAML config, cron-oriented README documentation, and tests that prove the configured one-shot path works end to end.

Purpose: The phase is only complete when a non-expert operator can run it manually or from cron using one documented entrypoint.
Output: CLI entrypoint, sample config, documentation, exports, and tests.
</objective>

<execution_context>
@/Users/wuwei/Documents/polymarketQuantification/.codex/get-shit-done/workflows/execute-plan.md
@/Users/wuwei/Documents/polymarketQuantification/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-automation-scheduled-reports/06-CONTEXT.md
@src/polymarket_quant/services/strategy_cli.py
@src/polymarket_quant/services/report_outputs.py
@README.md
</context>

<threat_model>
## Threat Model

| ID | Threat | Severity | Mitigation Required In This Plan |
|----|--------|----------|-----------------------------------|
| T-06-13 | Manual and cron-triggered runs use different code paths, causing drift | high | Expose one CLI/service path for both manual and cron execution |
| T-06-14 | Docs imply an internal scheduler or live-trading capability that does not exist | high | Keep README wording explicit about CLI + cron and `realtime_paper` only |
| T-06-15 | Sample config is incomplete or inconsistent with the implemented defaults | medium | Add a checked-in daily config sample validated by CLI tests |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Implement the automation CLI entrypoint</name>
  <files>src/polymarket_quant/services/automation_cli.py, src/polymarket_quant/services/__init__.py, tests/unit/test_automation_cli.py</files>
  <read_first>src/polymarket_quant/services/strategy_cli.py, src/polymarket_quant/services/automation_runner.py</read_first>
  <action>Create a one-shot CLI/service entrypoint that loads YAML config, runs the automation runner, and returns output paths/results suitable for manual use or cron invocation.</action>
  <verify>pytest tests/unit/test_automation_cli.py -q</verify>
  <acceptance_criteria>
    - `src/polymarket_quant/services/automation_cli.py` contains `class AutomationCliService`
    - `src/polymarket_quant/services/automation_cli.py` contains `main`
    - `tests/unit/test_automation_cli.py` contains `test_cli_runs_automation_from_yaml_config`
    - `tests/unit/test_automation_cli.py` contains `test_cli_returns_report_paths_and_status`
  </acceptance_criteria>
  <done>Phase 6 has one operator-facing automation entrypoint for manual and cron-driven use.</done>
</task>

<task type="auto">
  <name>Task 2: Add sample config and cron-focused documentation</name>
  <files>config/automation.daily.yaml, README.md, tests/unit/test_automation_cli.py</files>
  <read_first>.planning/phases/06-automation-scheduled-reports/06-CONTEXT.md, README.md</read_first>
  <action>Add a checked-in daily automation config example and document manual invocation plus cron usage in the README. Keep wording explicit that scheduling is external and automated strategy runs use `realtime_paper`, not live trading.</action>
  <verify>pytest tests/unit/test_automation_cli.py -q</verify>
  <acceptance_criteria>
    - `config/automation.daily.yaml` contains `realtime_paper`
    - `config/automation.daily.yaml` contains `report_formats`
    - `README.md` contains `cron`
    - `README.md` contains `automation`
    - `tests/unit/test_automation_cli.py` contains `test_readme_documents_cron_without_internal_scheduler_claim`
  </acceptance_criteria>
  <done>Phase 6 is documented as a usable scheduled-report workflow for a non-expert operator.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/unit/test_automation_cli.py -q` passes
- [ ] `pytest -q` passes
- [ ] `rg "AutomationCliService|main" src/polymarket_quant/services/automation_cli.py` finds the CLI entrypoint
- [ ] `rg "cron|realtime_paper|automation" README.md config/automation.daily.yaml` finds the documented scheduled workflow and its guardrails
</verification>

<success_criteria>
- The automation workflow has one standard CLI entrypoint
- A sample daily YAML config exists and matches Phase 6 defaults
- README teaches manual and cron usage without overstating capabilities
- Phase 6 ships as an operator-usable scheduled-report workflow
</success_criteria>

<output>
After completion, create `.planning/phases/06-automation-scheduled-reports/06-05-SUMMARY.md`
</output>
