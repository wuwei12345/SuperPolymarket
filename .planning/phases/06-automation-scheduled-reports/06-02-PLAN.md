---
phase: 06-automation-scheduled-reports
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src/polymarket_quant/services/automation_runner.py
  - src/polymarket_quant/services/automation_tasks.py
  - tests/unit/test_automation_runner.py
autonomous: true
requirements: [AUTO-01, AUTO-03]
user_setup: []
must_haves:
  truths:
    - The user explicitly chose graded failure semantics, not fail-fast and not pure best-effort.
    - Report generation must always be attempted, even when earlier steps fail.
    - Strategy execution should be skipped when prerequisites are not met, not forced through a broken environment.
  artifacts:
    - `automation_runner.py` orchestrates the default Phase 6 task chain.
    - `automation_tasks.py` exposes reusable task functions for sync, health, strategy batch, and report steps.
    - Unit tests prove failure, skip, and report-always semantics.
  key_links:
    - Report generation depends on truthful task results from this runner.
---

<objective>
Implement the lightweight Phase 6 runner and task orchestration layer that can execute the default automation chain with the user-approved graded failure policy.

Purpose: Scheduled automation is only trustworthy if task ordering, skip behavior, and failure handling are explicit and deterministic.
Output: automation runner, task wrappers, structured task results, and unit tests.
</objective>

<execution_context>
@/Users/wuwei/Documents/polymarketQuantification/.codex/get-shit-done/workflows/execute-plan.md
@/Users/wuwei/Documents/polymarketQuantification/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-automation-scheduled-reports/06-CONTEXT.md
@.planning/phases/06-automation-scheduled-reports/06-RESEARCH.md
@src/polymarket_quant/services/market_sync.py
@src/polymarket_quant/services/realtime_collector.py
@src/polymarket_quant/services/strategy_cli.py
</context>

<threat_model>
## Threat Model

| ID | Threat | Severity | Mitigation Required In This Plan |
|----|--------|----------|-----------------------------------|
| T-06-04 | A sync or health failure incorrectly aborts reporting, hiding the failure from operators | high | Always attempt report generation and persist failed task results |
| T-06-05 | Strategy batch runs execute in a broken environment because prerequisites are not checked | high | Gate strategy execution behind explicit prerequisite evaluation and emit `skipped` when needed |
| T-06-06 | The runner silently deviates from the locked default task chain | medium | Encode enabled-task ordering explicitly and test the default chain |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Implement automation task wrappers</name>
  <files>src/polymarket_quant/services/automation_tasks.py, tests/unit/test_automation_runner.py</files>
  <read_first>src/polymarket_quant/services/market_sync.py, src/polymarket_quant/services/realtime_collector.py, src/polymarket_quant/services/strategy_cli.py</read_first>
  <action>Create reusable task functions/services for market sync, realtime health check, strategy batch execution, and report generation handoff. Return structured task results rather than only side effects.</action>
  <verify>pytest tests/unit/test_automation_runner.py -q</verify>
  <acceptance_criteria>
    - `src/polymarket_quant/services/automation_tasks.py` contains `run_market_sync`
    - `src/polymarket_quant/services/automation_tasks.py` contains `run_realtime_health_check`
    - `src/polymarket_quant/services/automation_tasks.py` contains `run_strategy_batch`
    - `tests/unit/test_automation_runner.py` contains `test_strategy_batch_can_be_skipped_when_prerequisites_fail`
  </acceptance_criteria>
  <done>Phase 6 has reusable, structured task units for the automation chain.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the graded-failure automation runner</name>
  <files>src/polymarket_quant/services/automation_runner.py, tests/unit/test_automation_runner.py</files>
  <read_first>src/polymarket_quant/domain/automation.py, src/polymarket_quant/services/automation_tasks.py</read_first>
  <action>Create a lightweight runner that executes enabled tasks in order, records success/failure/skip per task, carries forward prerequisite state, and always attempts report generation. Keep it one-shot and cron-friendly.</action>
  <verify>pytest tests/unit/test_automation_runner.py -q</verify>
  <acceptance_criteria>
    - `src/polymarket_quant/services/automation_runner.py` contains `class AutomationRunner`
    - `src/polymarket_quant/services/automation_runner.py` contains `report_generation`
    - `src/polymarket_quant/services/automation_runner.py` contains `skipped`
    - `tests/unit/test_automation_runner.py` contains `test_runner_continues_after_health_failure_and_marks_report`
    - `tests/unit/test_automation_runner.py` contains `test_report_generation_is_always_attempted`
  </acceptance_criteria>
  <done>The automation runner now reflects the user's graded-failure policy instead of an ad hoc control flow.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/unit/test_automation_runner.py -q` passes
- [ ] `pytest -q` passes
- [ ] `rg "run_market_sync|run_realtime_health_check|run_strategy_batch" src/polymarket_quant/services/automation_tasks.py` finds task wrappers
- [ ] `rg "AutomationRunner|report_generation|skipped" src/polymarket_quant/services/automation_runner.py` finds required orchestration semantics
</verification>

<success_criteria>
- The default Phase 6 task chain is explicit and reusable
- Failures and skips are recorded truthfully
- Strategy execution is prerequisite-aware
- Report generation is always attempted
</success_criteria>

<output>
After completion, create `.planning/phases/06-automation-scheduled-reports/06-02-SUMMARY.md`
</output>
