---
phase: 06-automation-scheduled-reports
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/polymarket_quant/domain/automation.py
  - src/polymarket_quant/services/automation_config.py
  - src/polymarket_quant/services/__init__.py
  - tests/unit/test_automation_models.py
autonomous: true
requirements: [AUTO-01, AUTO-03]
user_setup: []
must_haves:
  truths:
    - Phase 6 needs a first-class automation run record; strategy manifests alone are not enough.
    - Resolved automation config must be persisted so scheduled runs remain reproducible after defaults change.
    - Task results must distinguish success, failure, and skip, because graded failure semantics are a core user decision.
  artifacts:
    - `automation.py` defines automation run, task result, and report reference contracts.
    - `automation_config.py` resolves YAML config into typed effective settings.
    - Unit tests lock serialization and resolved-default behavior.
  key_links:
    - Later runner, report, and CLI plans all depend on these domain/config models.
---

<objective>
Create the domain and config foundation for Phase 6: typed automation run models, per-task result models, report references, and resolved YAML config parsing.

Purpose: Every scheduled run must leave behind factual, reproducible evidence of what was attempted and with which effective configuration.
Output: automation domain models, config resolver, exports, and unit tests.
</objective>

<execution_context>
@/Users/wuwei/Documents/polymarketQuantification/.codex/get-shit-done/workflows/execute-plan.md
@/Users/wuwei/Documents/polymarketQuantification/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-automation-scheduled-reports/06-CONTEXT.md
@.planning/phases/06-automation-scheduled-reports/06-RESEARCH.md
@.planning/phases/06-automation-scheduled-reports/06-PATTERNS.md
@src/polymarket_quant/services/strategy_cli.py
@src/polymarket_quant/services/run_artifacts.py
</context>

<threat_model>
## Threat Model

| ID | Threat | Severity | Mitigation Required In This Plan |
|----|--------|----------|-----------------------------------|
| T-06-01 | Scheduled runs are not reproducible because only the config path is stored | high | Resolve and persist the effective automation config in typed models |
| T-06-02 | Later phases cannot explain why a task was not executed because skip/fail are conflated | high | Add explicit task-result status with `skipped` distinct from `failed` |
| T-06-03 | Automation metadata becomes ad hoc dicts with unstable fields | medium | Introduce strict typed automation domain contracts plus unit tests |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Define automation domain models</name>
  <files>src/polymarket_quant/domain/automation.py, tests/unit/test_automation_models.py</files>
  <read_first>.planning/phases/06-automation-scheduled-reports/06-CONTEXT.md, src/polymarket_quant/domain/operator.py, src/polymarket_quant/domain/strategy.py</read_first>
  <action>Create strict typed models for automation run identity, task status/result, report references, and run summary. Keep serialization explicit and ready for disk persistence.</action>
  <verify>pytest tests/unit/test_automation_models.py -q</verify>
  <acceptance_criteria>
    - `src/polymarket_quant/domain/automation.py` contains `AutomationRun`
    - `src/polymarket_quant/domain/automation.py` contains `AutomationTaskResult`
    - `src/polymarket_quant/domain/automation.py` contains `skipped`
    - `tests/unit/test_automation_models.py` contains `test_automation_run_serializes_resolved_config`
    - `tests/unit/test_automation_models.py` contains `test_task_result_distinguishes_failed_and_skipped`
  </acceptance_criteria>
  <done>Phase 6 has a typed automation contract that later services can rely on.</done>
</task>

<task type="auto">
  <name>Task 2: Implement YAML config resolution</name>
  <files>src/polymarket_quant/services/automation_config.py, src/polymarket_quant/services/__init__.py, tests/unit/test_automation_models.py</files>
  <read_first>src/polymarket_quant/services/strategy_cli.py, .planning/phases/06-automation-scheduled-reports/06-CONTEXT.md</read_first>
  <action>Create config loader/resolver logic that reads YAML, applies defaults, validates enabled tasks and default mode, and returns a resolved typed config ready to persist into the automation run record.</action>
  <verify>pytest tests/unit/test_automation_models.py -q</verify>
  <acceptance_criteria>
    - `src/polymarket_quant/services/automation_config.py` contains `load_automation_config`
    - `src/polymarket_quant/services/automation_config.py` contains `realtime_paper`
    - `src/polymarket_quant/services/automation_config.py` contains `report_formats`
    - `tests/unit/test_automation_models.py` contains `test_config_loader_applies_phase6_defaults`
    - `tests/unit/test_automation_models.py` contains `test_config_loader_rejects_unknown_default_tasks`
  </acceptance_criteria>
  <done>Phase 6 has a stable resolved-config boundary for manual and cron-driven runs.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/unit/test_automation_models.py -q` passes
- [ ] `pytest -q` passes
- [ ] `rg "AutomationRun|AutomationTaskResult|report_formats" src/polymarket_quant/domain/automation.py src/polymarket_quant/services/automation_config.py` finds required model fields
- [ ] `rg "realtime_paper|market_sync|report_generation" src/polymarket_quant/services/automation_config.py` finds locked Phase 6 defaults
</verification>

<success_criteria>
- Automation run identity and resolved config are typed
- Task results can represent success, failure, and skip separately
- YAML config resolves into a reproducible effective configuration
- Later runner/report/CLI plans have a stable contract to build on
</success_criteria>

<output>
After completion, create `.planning/phases/06-automation-scheduled-reports/06-01-SUMMARY.md`
</output>
