---
phase: 06-automation-scheduled-reports
plan: 04
type: execute
wave: 4
depends_on: [06-03]
files_modified:
  - src/polymarket_quant/services/report_outputs.py
  - src/polymarket_quant/services/report_builder.py
  - tests/unit/test_report_index.py
autonomous: true
requirements: [AUTO-02, AUTO-03]
user_setup: []
must_haves:
  truths:
    - HTML is a derived Phase 6 output; Markdown remains canonical.
    - Reports need stable file locations and an index/history so operators can find prior runs.
    - Report outputs must stay disk-based in this phase; no external push channels.
  artifacts:
    - `report_outputs.py` writes Markdown and HTML files plus a lightweight index/history entry.
    - Unit tests prove HTML mirrors canonical content and report paths are stable.
  key_links:
    - The CLI plan depends on this output layer to expose final report locations cleanly.
---

<objective>
Add report output persistence for Phase 6: write Markdown and HTML files to stable dated locations and maintain a lightweight report index/history for operators and later automation runs.

Purpose: A report is only useful if it is easy to locate, compare, and reference after the automation run finishes.
Output: report output writer/index helpers and unit tests.
</objective>

<execution_context>
@/Users/wuwei/Documents/polymarketQuantification/.codex/get-shit-done/workflows/execute-plan.md
@/Users/wuwei/Documents/polymarketQuantification/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-automation-scheduled-reports/06-CONTEXT.md
@.planning/phases/06-automation-scheduled-reports/06-RESEARCH.md
@src/polymarket_quant/services/run_artifacts.py
@src/polymarket_quant/services/report_builder.py
</context>

<threat_model>
## Threat Model

| ID | Threat | Severity | Mitigation Required In This Plan |
|----|--------|----------|-----------------------------------|
| T-06-10 | HTML and Markdown diverge because they are generated from separate content paths | medium | Derive HTML from canonical Markdown or the same shared report context |
| T-06-11 | Operators cannot locate prior reports because file naming is unstable | high | Use explicit dated directory/file conventions and write an index entry |
| T-06-12 | Report storage grows ambiguous with no run-to-report linkage | medium | Persist report references back into automation output records/index rows |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Implement report output writing</name>
  <files>src/polymarket_quant/services/report_outputs.py, tests/unit/test_report_index.py</files>
  <read_first>src/polymarket_quant/services/report_builder.py, src/polymarket_quant/services/run_artifacts.py</read_first>
  <action>Create output helpers that write Markdown and HTML files under stable report directories keyed by date/run identity. Keep Markdown canonical and generate HTML from the same content path.</action>
  <verify>pytest tests/unit/test_report_index.py -q</verify>
  <acceptance_criteria>
    - `src/polymarket_quant/services/report_outputs.py` contains `write_report_outputs`
    - `src/polymarket_quant/services/report_outputs.py` contains `.html`
    - `src/polymarket_quant/services/report_outputs.py` contains `.md`
    - `tests/unit/test_report_index.py` contains `test_report_outputs_write_markdown_and_html`
  </acceptance_criteria>
  <done>Phase 6 can persist both canonical and derived report files in a stable layout.</done>
</task>

<task type="auto">
  <name>Task 2: Add report index/history support</name>
  <files>src/polymarket_quant/services/report_outputs.py, tests/unit/test_report_index.py</files>
  <read_first>src/polymarket_quant/domain/automation.py, src/polymarket_quant/services/report_outputs.py</read_first>
  <action>Persist a lightweight report index or history record linking automation run identity, window, report file paths, and summary status so operators can discover recent reports without scanning the filesystem manually.</action>
  <verify>pytest tests/unit/test_report_index.py -q</verify>
  <acceptance_criteria>
    - `src/polymarket_quant/services/report_outputs.py` contains `write_report_index`
    - `src/polymarket_quant/services/report_outputs.py` contains `window_start`
    - `tests/unit/test_report_index.py` contains `test_report_index_tracks_run_and_output_paths`
    - `tests/unit/test_report_index.py` contains `test_html_output_preserves_core_sections`
  </acceptance_criteria>
  <done>Phase 6 report outputs are stable, discoverable, and linked back to automation runs.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/unit/test_report_index.py -q` passes
- [ ] `pytest -q` passes
- [ ] `rg "write_report_outputs|write_report_index" src/polymarket_quant/services/report_outputs.py` finds output/index helpers
- [ ] `rg "\\.md|\\.html|window_start" src/polymarket_quant/services/report_outputs.py` finds required output fields
</verification>

<success_criteria>
- Markdown and HTML report files are written to stable paths
- HTML stays aligned with canonical Markdown content
- Recent reports can be discovered through an index/history record
- Report outputs remain disk-based and traceable
</success_criteria>

<output>
After completion, create `.planning/phases/06-automation-scheduled-reports/06-04-SUMMARY.md`
</output>
